# P0-GOV：治理骨架

状态：`done`  
负责人：`governance-agent`  
风险：`R1`  
关联目标：`P0-GOV-01` 至 `P0-GOV-06`  
更新时间：`2026-09-09`

## 结果与非目标

- 结果：建立可追溯的多 Agent 工作内核、私密数据边界、共享契约锁、风险模型、真实环境基线和依赖复用记录。
- 非目标：不创建或修改 `contracts/`、`src/`、`tests/`、`pyproject.toml`；不处理真实微信文件，不实施删除器。

## 前置条件与输入

- 依赖：空 Git 仓库；本机已有只读历史扫描结果。
- 输入及其私密等级：微信版本和容量仅以聚合事实形式记录；真实路径、账号、联系人、密钥和媒体不写入仓库。
- 要取得的共享契约锁：无；治理文件由协调 Agent 独占。

## 实施步骤

- [x] 建立 `AGENTS.md`、目标模板和追加式事实日志。
- [x] 建立 `agents.yaml`、`targets.yaml`、集成看板、风险模型及契约锁台账。
- [x] 配置真实数据、密钥、解码产物、缓存与收据的 Git 排除规则。
- [x] 记录微信 4.1.13.12、运行状态和 92.31 GiB 聚合基线。
- [x] 记录本机可见复用候选的许可证及“仅评估，不复制”的边界。
- [x] 进行 YAML/链接/忽略规则自检。

## 验收

- [x] 可重复验证命令：`git check-ignore -v private/key_info.db decoded/sample.jpg`。
- [x] 预期结果：两类私密路径均命中 `.gitignore`；治理 YAML 可被 YAML 解析器加载。
- [x] 失败/边界情形：若未来需要真实夹具，必须放入已忽略的 `fixtures/private/`，不得以脱敏文件名掩盖真实内容。

## 风险与回滚

- 风险：治理规则无法从技术上阻止人工强制提交；高风险删除实现可能绕过计划/收据流程。
- 回滚：规则文件和事实日志为文本，可通过后续受审查的提交修订；真实数据从未写入仓库，无需数据回滚。
- 需要人工决定：未来公开发布前，需单独审查微信兼容性、逆向相关法律风险和第三方依赖许可证。

## 交接

- 改动文件：`AGENTS.md`、`README.md`、`.gitignore`、`plan/*`、`coordination/*`、`docs/*`。
- 事实日志条目：`F-20260909-001` 至 `F-20260909-004`。
- 契约锁状态：无锁持有。
- 未解决项：确定实际采用的数据库/图像解码库后，补齐 SPDX 清单与 NOTICE。
