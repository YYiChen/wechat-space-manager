# P1-INTEGRATION：契约与夹具集成

状态：`done`  
负责人：`orchestrator`  
风险：`R1`  
关联目标：`P1-CONTRACT-01, P1-FIXTURE-01`  
更新时间：`2026-09-09`

## 结果与非目标

- 结果：核对 Phase 1 公开契约、JSON Schema、合成夹具与私有验收夹具的隔离规则，并以仓库级静态检查和测试作为完成证据。
- 非目标：不读取真实微信目录、不调用解码器、不执行任何清理操作。

## 前置条件与输入

- 依赖：`P0-GOV-01`、`P0-GOV-03`、`P0-GOV-04` 已完成。
- 输入及其私密等级：仅仓库内公开代码、合成数据和本机私有夹具的忽略规则；私有夹具内容不进入验证输出。
- 要取得的共享契约锁：无。契约实现已完成并处于空闲锁状态；本次只验证及登记版本状态。

## 实施步骤

- [x] 核对公开模型覆盖账号、联系人、扫描、媒体映射、解码、清理计划/收据、错误码和兼容策略。
- [x] 核对合成目录、脱敏契约数据与本机私有验收夹具模板的边界。
- [x] 执行 Ruff、pytest、compileall、私密路径忽略检查和 Git 空白错误检查。
- [x] 将两个 Phase 1 目标、共享锁兼容状态和事实证据登记到协调文件。

## 验收

- [x] 可重复验证命令：`python -m ruff check src tests contracts`、`python -m pytest -q`、`python -m compileall -q src tests contracts`。
- [x] 预期结果：静态检查通过，12 个测试通过，编译成功；私有夹具和虚拟环境不被 Git 跟踪。
- [x] 失败/边界情形：契约拒绝越界相对路径、低/无映射置信度清理目标和未经确认的永久清理计划；合成夹具不包含真实聊天内容或密钥。

## 风险与回滚

- 风险：未来消费者改变公共序列化字段会破坏 v1。
- 回滚：维持 `1.x` 兼容读取；破坏性变化另起主版本和迁移夹具，禁止修改既有收据语义。
- 需要人工决定：后续 Phase 2 选择哪一个经过许可证复核的本地解码适配器。

## 交接

- 改动文件：`coordination/targets.yaml`、`coordination/contract-lock.yaml`、`coordination/integration-board.md`、`coordination/fact-log.md`。
- 事实日志条目：`F-20260909-006`。
- 契约锁状态：全部空闲；v1 契约已在当前生产者/夹具范围内验证，消费者实现尚未开始。
- 未解决项：真实微信版本适配、数据库副本读取和非 Agent 清理执行器均在后续阶段。
