# 事实日志

规则：只追加；每条事实应说明证据、范围和不能推导出的内容。不得记录聊天内容、账号标识、密钥、原始路径或媒体文件名。

| ID | 日期 | 事实 | 证据与范围 | 不可推导的结论 |
| --- | --- | --- | --- | --- |
| F-20260909-001 | 2026-09-09 | 仓库已初始化，Phase 0 治理文件已建立。 | 当前 Git 工作树与本目录文件检查。 | 不代表任何应用功能已实现。 |
| F-20260909-002 | 2026-09-09 | 多 Agent 所有权、目标状态、契约锁和集成门禁已定义；所有初始契约锁为空闲。 | `coordination/agents.yaml`、`targets.yaml`、`contract-lock.yaml`。 | 不代表 Phase 1 契约已定义。 |
| F-20260909-003 | 2026-09-09 | 私密数据库、密钥、真实夹具、解码产物、缓存和清理收据的主要路径已被 Git 忽略。 | `.gitignore` 规则及 `git check-ignore` 自检。 | `.gitignore` 不能替代人工审查，也不能移除已经被 Git 跟踪的文件。 |
| F-20260909-004 | 2026-09-09 | 本机观察到微信 4.1.13.12 正在运行；历史只读盘点的聚合总量为 92.31 GiB。候选 `wechatauto-replica` 工作副本为 Apache-2.0，revision `798989c9b61066c120b59ceba636b557b776ff7f`。 | Windows 卸载信息和可执行文件版本；先前只读盘点聚合；本机候选副本的 `LICENSE` 和 Git revision。 | 未确认该候选适合本项目、所有功能可复用或可公开分发；基线不是实时空间读数。 |
| F-20260909-005 | 2026-09-09 | 治理自检通过：治理 YAML 可解析；私密夹具、密钥和解码产物路径被忽略；示例夹具路径保持可跟踪。 | Python YAML 加载、`git diff --check`、`git check-ignore --no-index`。 | 不代表 Git 自动防止人工强制提交或未来实现符合所有规则。 |
| F-20260909-006 | 2026-09-09 | Phase 1 的 v1 公开契约、10 份 JSON Schema、合成文件树、私有验收模板及其隔离规则已通过仓库级验证。 | `python -m ruff check src tests contracts` 通过；`python -m pytest -q` 为 12 passed；`python -m compileall -q src tests contracts` 通过；私有夹具和虚拟环境由 Git 忽略规则覆盖。 | 不代表已支持真实微信目录、真实数据库解码、联系人映射准确性或任何清理执行。 |
| F-20260909-007 | 2026-09-09 | 并行开发所有权已校正为实际 `wechat_cleaner` 包路径，并登记 fixtures Agent；Phase 2 已拆分为五个可独立实现模块和一个集成目标。 | `coordination/agents.yaml`、`coordination/targets.yaml`、`plan/phase-2-parallel-modules.md`；所有模块代码和测试目录互不重叠。 | 目标仍为 planned；尚未实现扫描、解码、映射、UI 或清理执行能力。 |
| F-20260909-008 | 2026-09-09 | 项目仓库已从工作区深层目录迁移至当前短路径仓库根；迁移后全套验证仍通过。 | 迁移前后路径存在性/源路径消失检查；新仓库 `ruff` 通过、12 tests passed、compileall 通过。 | 不代表用户真实微信目录或已解码产物发生任何迁移。 |
| F-20260909-009 | 2026-09-09 | 迁移后发现两个测试遗留的合成空目录树；逐树确认不含文件后已移除。 | 目录递归文件数均为 0；删除目标位于项目仓库内且不含真实数据。 | 不代表任何真实微信目录、数据库或解码产物被删除。 |
| F-20260909-010 | 2026-09-09 | 事实日志审查发现迁移记录 F-20260909-008 包含项目绝对路径；该路径不属于微信私密数据，但不符合日志的最小暴露规则，后续记录改用逻辑仓库根描述。 | 本次仅新增说明，不改写既有事实；私密数据、账号标识、媒体文件名和真实微信绝对路径未被记录。 | 不影响仓库迁移或验证结果。 |
| F-20260909-011 | 2026-09-09 | 迁移后的仓库根目录中两个测试缓存已清除；虚拟环境保留。 | 目标仅为项目内可再生的 pytest/Ruff 缓存目录；删除前确认目标在仓库内。 | 不代表清理过任何微信目录、用户文件或解码产物。 |
| F-20260909-012 | 2026-09-09 | P2-MAPPER-01 已完成：规范化证据映射、EXACT/HIGH/MEDIUM/LOW/UNMAPPED 置信度、账号边界、歧义拒绝和路径验证均有测试。 | mapper 目标测试为 8 passed；mapper 目录 Ruff 通过；`compileall -q src tests contracts` 通过。全仓 Ruff/pytest 仍被 decoder 分支的既有收集错误和静态错误阻塞，未修改 decoder。 | 不代表真实数据库适配器、真实微信版本映射或清理执行已经实现。 |
| F-20260909-012 | 2026-09-09 | scanner 已完成只读账号发现、文件身份枚举、保守分类、保护目录标记和重解析点拒绝；scanner 专项验证通过。 | `ruff check src/wechat_cleaner/scanner tests/scanner` 通过；`pytest tests/scanner -q` 为 4 passed；排除 decoder 的全仓回归为 16 passed；scanner 与测试编译通过。 | 全仓测试尚未完成，原因是 decoder 测试依赖 Pillow 但当前开发环境未声明/安装；不代表 decoder 或集成层已验收。 |
| F-20260909-013 | 2026-09-09 | 全仓静态检查仍被 decoder 工作区改动阻断；scanner 路径未出现该类错误。 | 全仓 Ruff 报告 11 个 decoder 相关问题；scanner 专项 Ruff 仍通过；未修改 decoder 文件。 | 不代表 decoder 问题已修复，也不改变 scanner 的专项验收结果。 |
| F-20260909-014 | 2026-09-09 | scanner 安全加固后仍通过专项和非 decoder 回归；当前非 decoder 测试数量已增至 24。 | `pytest -q --ignore=tests/decoder` 为 24 passed；scanner Ruff 通过。 | 全仓门禁仍等待 decoder 依赖和静态问题处理。 |
| F-20260909-015 | 2026-09-09 | P2-MAPPER-01 的规范化证据映射已完成：支持路径/哈希精确映射、目录前缀高置信映射、名称日期大小中置信相关、低置信提示、无映射和歧义拒绝，并执行账号边界与路径验证。 | mapper 专项 `pytest` 为 8 passed；mapper 目录 Ruff 通过；mapper 与测试 `compileall` 通过。 | 全仓门禁仍受 decoder 工作区错误阻塞；不代表真实数据库 adapter 或清理执行已完成。 |
| F-20260909-016 | 2026-09-09 | 并发 Agent 曾同时使用事实日志 ID `F-20260909-012`；本条及 `F-20260909-015` 是 mapper 的唯一可引用验收证据，后续由 integration Agent 统一分配事实 ID。 | 对照 `coordination/integration-board.md` 与事实日志末尾条目。 | 不改写此前已追加的事实；重复 ID 不能作为新的集成证据。 |
| F-20260909-021 | 2026-09-09 | P2-INTEGRATION-01 已建立合成 scanner→mapper→UI 筛选/汇总→DecodeRequest/CleanupPlan 契约 smoke 流，5 个集成测试通过；compileall 通过。 | `python scripts/verify_integration.py --allow-pending`：integration_tests 当前因 decoder 测试收集缺少 Pillow 失败；owned_static_checks 因 UI 既有 Ruff 问题失败；`tests/integration -q` 为 5 passed。 | 不代表 decoder、cleanup_safety 或 UI 的专项质量已完成，也不代表全仓门禁通过。 |
| F-20260909-020 | 2026-09-09 | P2-CLEANUP-01 已实现无副作用清理安全层：根据置信度、保护目录和缓存显式确认构造不可变 `CleanupPlan`；执行前复核微信运行状态、账号根目录、重解析点、路径包含、文件类型、大小、修改时间和可选 SHA-256；逐目标规范化 `CleanupReceipt`，不提供删除入口。 | cleanup 专项 `ruff` 通过；`pytest tests/cleanup -q` 为 13 passed、1 个 symlink 能力软跳过；全仓 `pytest -q` 为 94 passed、1 个同类软跳过；`compileall -q src tests contracts` 通过；全仓 Ruff 当前被 UI 自有文件的 10 个既有格式问题阻塞。测试仅使用临时合成目录，无真实微信路径、数据库、聊天内容或密钥。 | 不代表独立执行器、UI、真实微信版本兼容或永久删除已实现；全仓 Ruff 需 UI 目标修复后重新运行。 |
| F-20260909-017 | 2026-09-09 | P2-DECODER-01 已完成：副本解码管线（明文与 XOR 混淆图像、单字节密钥自动推导、多字节密钥仅经内存回调）、像素验证与缩略图缩放、受限会话缓存（总额/单文件限额、过期清除、路径包含检查）、密钥用后零化且测试断言不落盘，全部失败路径映射契约错误码。 | decoder 专项 `pytest tests/decoder -q` 为 25 passed；全仓 `ruff check src tests contracts` 通过；全仓 `pytest tests -q` 为 49 passed；`compileall -q src tests contracts` 通过。环境事实：原虚拟环境的 site-packages 已损坏为空，按治理文档基线重建（Python 3.12）并安装 editable 项目、pytest、ruff、Pillow 12.3.0；损坏环境已更名保留待项目所有者确认后清理。 | 不代表微信 4.x 真实图像格式已适配（需私有夹具人工验收）；Pillow 尚未声明进 `pyproject.toml`（该文件归协调 Agent）；全仓通过不代表清理执行能力已实现。 |
| F-20260909-018 | 2026-09-09 | decoder 测试完成协作加固：发现扁平测试目录下裸 `from conftest import` 会被其他 Agent 目录的 conftest 抢占模块名（全仓收集时 decoder 测试解析到 scanner 目录的 conftest）；decoder 测试已全部改为 fixture 注入与本文件常量，不再跨目录导入。 | 全仓 `ruff check src tests contracts` 通过；全仓 `pytest tests -q` 为 50 passed；`compileall` 通过。 | 该问题是布局级风险，其他 Agent 的测试目录同样可能互相抢占；是否统一测试包布局需协调 Agent 决定，不由 decoder 单方改动。 |
| F-20260909-019 | 2026-09-09 | 当前 decoder、scanner、mapper 在合成数据范围内可协同运行：三模块专项共 38 passed，全仓 50 passed，静态检查和编译通过；scanner 候选可转换为 mapper 候选并产出 15 条 `MediaRecord`。 | `ruff check src tests contracts` 通过；`pytest tests/scanner tests/mapper tests/decoder -q` 为 38 passed；`pytest -q` 为 50 passed；合成 scanner→mapper smoke 为 2 accounts/15 candidates passed；未读取真实微信目录。 | 不代表真实微信数据库适配、真实 4.x 加密格式覆盖、UI 或清理执行已验收；Pillow 目前只在本机虚拟环境安装，尚未写入项目依赖声明。 |
| F-20260909-022 | 2026-09-09 | P2-UI-01 已完成只读审查与缓存控制界面：`filters`（MediaRecord 纯函数筛选/排序/汇总）、`preview`（DecodeRequest 构造与 decode 消费，失败转 `PreviewOutcome.error` 不上抛；`open_artifact` 限定缓存根内）、`cache_control`（用量/会话统计、过期清除透传 decoder API、限额与可回收报告）、`cli`（browse/preview/cache 三子命令，`--json` 机器可读，exit 0/1/2）。 | ui 专项 `pytest tests/ui -q` 为 29 passed；全仓 `ruff check src tests contracts` 通过；全仓 `pytest -q` 为 94 passed、1 skipped（symlink 用例跳过属 cleanup 范围）；`compileall -q` 通过。UI 不含任何删除微信文件的路径，输出摘要仅含相对路径。 | 不代表 GUI 形态已定型（v1 为 CLI 零新依赖）；`__main__` 挂接与 CleanupPlan 展示待 orchestrator/集成阶段决定；CLI 输入的本机路径由用户显式提供，不构成微信私密数据泄露。 |
| F-20260909-023 | 2026-09-09 | P2-INTEGRATION-01 的完整集成门禁在隔离验证环境中通过：跨模块测试、全仓静态检查和编译均成功；仅 cleanup 的 symlink 用例因当前 Windows 能力跳过。 | `scripts/verify_integration.py --full`：94 passed、1 skipped；owned/full Ruff 通过；compileall 通过。验证环境额外安装 Pillow 12.3.0 以运行图像夹具，未修改项目依赖声明、业务模块或真实微信数据。 | 不代表 `pyproject.toml` 已声明 Pillow，干净环境仍可能在 decoder/UI 测试收集阶段缺少该依赖；也不代表真实微信格式、真实路径或永久清理已验收。 |
| F-20260909-024 | 2026-09-09 | Phase 3 桌面 Beta 已建立可追溯的目标、独立路径所有权、契约锁、机器可读依赖波次、逐目标计划和多 Agent 派工规则；Phase 2 状态已在目标表与看板收口。 | `plan/phase-3-desktop-beta.md`、14 个 `P3-*` 计划、`coordination/phase-3-waves.yaml`、`phase-3-dispatch.md` 及 v2 治理 YAML；本次仅编辑治理与文档。 | 不代表 GUI、SQLite 索引、数据库适配器或可恢复执行器已经实现；仓库尚无首个 Git 基线提交，不能据此安全启动并行 worktree，也未读取或处理任何真实微信数据。 |
| F-20260909-025 | 2026-09-09 | Phase 3 规划落盘后治理一致性和既有 Phase 2 功能门禁均通过。 | 5 个治理 YAML 解析成功；28 个目标 owner/依赖有效且依赖图无环；14 个 Phase 3 计划存在；28 行集成看板列数一致；`scripts/verify_integration.py --full` 为 94 passed、1 skipped，Ruff 与 compileall 通过。 | 该证据只验证规划结构和既有合成功能，没有实现或验收 Phase 3 GUI、执行器、真实微信兼容与私有清理。 |
| F-20260909-026 | 2026-09-09 | P3-GOV-01 已完成：Phase 2 状态和依赖收口，首个用户归属基线提交建立，并成功创建/移除独立 worktree。 | 全新的隔离 venv 安装项目 `[dev]` 成功；`scripts/verify_integration.py --full` 为 99 passed、1 skipped；基线提交 `a3fef5ae0bf49063c2e9c1c2e15fd983e7eb841d` 作者为用户确认身份；临时 worktree 指向同一提交且移除后主工作树干净。 | 不代表 Phase 3 GUI、数据库适配器、索引、执行器或真实微信兼容已经实现。 |
| F-20260909-027 | 2026-09-09 | P3-ENTRY-01 的入口分流和依赖声明已完成可验证部分：无参数/`--gui` 进入桌面加载路径，`legacy`/`--legacy-cli` 保留只读 CLI，缺 GUI 模块时返回可操作错误。 | `tests/entrypoint -q` 为 5 passed；入口 Ruff 和 compileall 通过；全新环境的 `wechat-space-manager --help`、`legacy --help` 返回 0，无参数在 GUI 尚未提供时返回 2 并提示后续安装/接线。 | `P3-GUI-FLOW-01` 尚未提供 `wechat_cleaner.gui:main`，因此尚未证明真实 GUI 窗口启动/退出；入口不绕过 application facade。 |
| F-20260909-028 | 2026-09-09 | P3-ENTRY-01 的 GUI、测试和打包 extras 可在全新环境解析。 | `pip install --dry-run -e ".[gui,test,package]"` 成功解析 PySide6、pytest-qt、PyInstaller 及其依赖；未安装大型 extras，未接触真实微信数据。 | 依赖可解析不等于 GUI 已实现或 Windows 打包产物已验收；真实 GUI 启动仍等待 `P3-GUI-FLOW-01`。 |
| F-20260909-029 | 2026-09-09 | P3-CONTRACT-01 已完成：四组 Phase 3 数据契约（扫描 lineage、筛选/选择、数据库证据、可恢复执行）落地，新增 v1.1 契约并保持 v1.0 输入/schema 兼容；22 份公开 JSON Schema、合成夹具和消费者失败路径测试已同步。 | 隔离验证环境最终门禁：`106 passed, 1 skipped`（Windows symlink 能力软跳过）；Ruff、compileall 和 schema 生成通过；`coordination/contract-lock.yaml` 中四个锁均为 inactive。 | 仅使用合成数据；未读取或写入真实微信目录、数据库、聊天内容或密钥。下游索引、数据库适配器、GUI 和执行器仍需按各自目标接入这些契约。 |
| F-20260909-030 | 2026-09-09 | P3-FIXTURE-01 已完成：新增带 seed 的 Phase 3 合成夹具生成器，覆盖双账号、direct/group、多年份月份、媒体/大小/状态/置信度/保护/重复维度，并生成两个支持和一个拒绝的 SQLite schema。 | `pytest tests/fixtures_synthetic/test_phase3_fixtures.py -q` 为 4 passed；fixture 专项 Ruff 通过；CLI 生成检查输出 15 records、14 materialized files、2 supported/1 rejected databases；同 seed 的 JSON、SQLite 字节和摘要一致。 | 生成内容仅供临时测试，清单只含合成 ID 和相对路径；未读取或提交真实微信目录、数据库、联系人、正文、密钥或绝对路径。 |
| F-20260909-031 | 2026-09-09 | P3-INDEX-01 已完成：新增本机私密元数据 SQLite 索引，支持 v1→v2 事务迁移、流式批量 upsert、FilterSpec 组合查询、digest 绑定的稳定游标分页、分组聚合、统计和删除索引文件式清除。 | `pytest tests/local_index -q` 为 7 passed；100,000 条合成记录流式导入、分页无重不漏、查询低于 300 ms 断言通过；Ruff 和 compileall 通过；排除并行未提交 `tests/filtering` 后仓库门禁为 117 passed、1 skipped。 | 索引只写入临时 SQLite 元数据，不接触微信源目录；未做真实磁盘耗尽测试。并行筛选 Agent 的 3 个未提交专项失败未纳入本目标范围。 |
| F-20260909-032 | 2026-09-09 | P3-FILTER-01 已完成：新增纯内存精确筛选引擎，支持组内 OR/组间 AND、稳定排序/游标分页、保留策略、资格判定、结构化排除和 `CURRENT_PAGE`/`CURRENT_RESULT`/`MANUAL` 选择快照。 | `pytest tests/filtering -q` 为 7 passed；筛选模块与测试 Ruff 通过；`compileall` 通过；全仓门禁仅受并行 INDEX 专项一次 316.5 ms（目标 <300 ms）性能波动影响，未修改 INDEX 代码。 | include 标志只改变可见性，不能绕过 protected/retained/eligibility；未读取或写入真实微信目录、聊天内容、数据库或密钥。 |
| F-20260909-033 | 2026-09-09 | P3-GUI-SHELL-01 已落地：PySide6 可选入口、中文四页桌面壳、分页/排序/条件编辑、线程池任务 token、取消、错误恢复和会话缓存控制均通过公开 facade port；GUI 源码无 executor 或文件删除/移动调用。 | `src/wechat_cleaner/gui/`、`tests/gui/test_gui_shell.py`；Ruff 与 compileall 通过；隔离环境 `pytest -q` 为 124 passed、3 skipped，其中 2 个 Qt 专项因 PySide6 未装入而显式跳过，1 个 cleanup symlink 能力跳过。 | 未据此推导 PySide6 offscreen 或真实 Windows DPI smoke 已通过；未读取或写入真实微信数据。 |
| F-20260909-034 | 2026-09-09 | P3-GUI-FLOW-01 已实现公开门面接线和安全状态机：当前页/全部结果/手选、计划摘要、预检、Dry Run、明确确认、收据/恢复入口，以及计划变化使确认失效；默认 fake facade 始终不执行文件操作。 | `src/wechat_cleaner/gui/services.py`、`src/wechat_cleaner/gui/window.py`、`tests/gui/test_gui_flow.py`；静态检查与全仓回归通过，Qt 专项因缺失 PySide6 显式跳过。 | `P3-WORKFLOW-01` 尚为 planned，因此真实 application facade、执行器和真实 Windows smoke 尚未验收；本条不代表删除或移动过任何文件。 |
| F-20260909-035 | 2026-09-09 | GUI 增加记录详情和按需预览请求，仍只经过 `ApplicationFacadePort`；默认门面只返回占位说明，不调用解码器、不写入缓存。 | `src/wechat_cleaner/gui/services.py`、`src/wechat_cleaner/gui/window.py`、`tests/gui/test_gui_shell.py`；Ruff、compileall 和非 Qt 回归通过。 | 未据此推导真实预览图像或 Qt offscreen 运行已验收。 |
| F-20260909-036 | 2026-09-09 | P3-DBADAPTER-01 已完成：新增只读数据库副本适配器，显式支持 synthetic-v1/v2，按账号参数化查询并输出无正文/无密钥的 `DatabaseEvidenceEnvelope`，在读取前后复核副本身份并报告冲突。 | `pytest tests/db_adapter -q` 为 4 passed；db_adapter 专项 Ruff 和 compileall 通过；测试覆盖拒绝 schema、损坏库、跨账号过滤、重复媒体冲突和绝对路径不泄露。 | 仅验证合成 SQLite；未打开真实微信活动数据库、未提取密钥、未读取真实聊天内容。 |
| F-20260909-037 | 2026-09-09 | P3-EXECUTOR-01 已完成：新增不可变计划执行器，支持 Dry Run、受控隔离/回收存储、账号与计划摘要门禁、微信运行阻止、TOCTOU 复核、幂等重试、同名冲突、部分失败、恢复和收据持久化 fail-closed；永久删除不存在。 | `pytest tests/executor -q` 为 5 passed；全仓 `ruff`、compileall 和 `pytest -q` 为 133 passed、3 skipped；测试只处理临时合成目录。 | `RECYCLE_BIN` 在本阶段使用显式受控恢复根以保证可恢复和可测试；未处理真实微信路径，未调用永久删除或系统级真实回收站。 |
| F-20260909-038 | 2026-09-09 | P3-WORKFLOW-01 已完成：新增 GUI 唯一可用的无头 application facade，串接扫描、数据库副本导入、映射、索引、筛选/选择、计划、预检、Dry Run、显式确认、收据恢复和预览请求；取消在提交前 fail-closed，重开 facade 只能查询索引，未重新扫描不能生成可执行计划。 | `pytest tests/application -q` 为 6 passed；全仓 `ruff check src tests`、compileall 和 `pytest -q` 为 139 passed、3 skipped；测试覆盖成功/恢复、取消、不支持 schema、过期选择、预检漂移、部分失败和索引重开，全部只用合成临时目录。 | 未读取、写入或清理真实微信目录、数据库、聊天内容或密钥；未据此推导 PySide6 offscreen 或真实 Windows GUI smoke 已通过。 |
| F-20260909-039 | 2026-09-09 | Phase 3 验收重新划分为 AI 合成门禁、AI 无私有数据就绪判定和 human-only 私有验收；项目路径与结构手册、机器可读就绪检查器和三类边界测试已落地。 | `pytest tests/integration -q` 为 12 passed；就绪检查默认返回 0 并输出 `blocked`，`--require-ready` 返回 2；5 个治理 YAML 可解析；检查输出明确 `private_data_accessed=false`。 | `blocked` 是当前前置目标未收口的准确状态，不代表 private 验收失败；本次未探测、读取、复制、解码或清理任何真实微信数据。 |
| F-20260909-040 | 2026-09-09 | P3-GUI-FLOW-01 已通过公开合成验收：桌面默认使用真实 application facade，Fake facade 仅用于显式 demo/测试；GUI 通过 facade 完成选择、计划、预检、Dry Run、确认、收据和恢复入口。 | 提交 `c3810cdc6cf2bcdf9380a41edfa78a478921b080`；`QT_QPA_PLATFORM=offscreen` 下 `pytest tests/gui -q` 为 10 passed；全仓 `pytest -q` 为 156 passed、1 skipped；Ruff、格式检查和 compileall 通过。 | 仅验证合成夹具和空索引路径；不代表真实微信私有验收、真实 Windows 数据兼容或任何文件已被删除/移动。 |
| F-20260909-041 | 2026-09-09 | P3-ENTRY-01 已通过入口和干净依赖验收：无参数启动真实 GUI，`legacy` 与 `--legacy-cli` 保留只读分流和退出码，Fake facade 只能显式演示；GUI/test extras 可在干净环境安装。 | 新 Python 3.12 venv 执行 `pip install -e .[gui,test]` 成功，解析 PySide6 6.11.2 与 pytest-qt 4.5.0；`pytest tests/entrypoint -q` 为 6 passed；无参数 Qt offscreen 启动/退出 smoke 返回 0；Ruff、格式检查和 compileall 通过。 | 未读取或操作真实微信目录、数据库、密钥、聊天内容或媒体；P3-INTEGRATION-01 完整门禁仍需集成 Agent 复核。 |
| F-20260909-042 | 2026-09-09 | P3-INTEGRATION-01 严格公开/合成门禁通过，所有必需检查返回 passed，目标具备 done 证据。 | `python scripts/verify_phase3.py`（无放宽参数）返回 0；治理、Ruff、compileall、契约/夹具 20 passed、scanner/mapper/decoder 38 passed、索引/数据库/筛选 18 passed、100k 性能 1 passed、清理/执行器/工作流 24 passed、集成 12 passed、入口 6 passed、Qt offscreen 10 passed、全仓 157 passed；清理专项和全仓各有 1 个 Windows symlink 能力软跳过。 | 仅使用公开代码、合成夹具和临时目录；未读取或操作真实微信目录、数据库、密钥、聊天内容或媒体；不代表 private 验收或发布包已完成。 |
| F-20260909-043 | 2026-09-09 | P3-PRIVATE-ACCEPT-01 的 AI 就绪检查已输出 `ready_for_human`，AI 工作在人工边界前停止。 | `python scripts/check_private_acceptance_readiness.py` 输出 `state=ready_for_human`、`ai_private_execution_allowed=false`、`private_data_accessed=false`；目标表、模板和 Git 私密路径检查通过。 | 不代表用户已经执行私有验收、隔离或恢复；P3-PRIVATE-ACCEPT-01 仍保持 `blocked`，直到用户提供匿名聚合结果。 |
| F-20260910-001 | 2026-09-10 | P4-GOV-01 完成：六个 P4 目标与一个人工目标已登记进目标表与看板；新模块路径已补所有权；Phase 4 波形与派工手册已建立；推进总计划与结果模板已落地。用户明确授权由单一 Agent 代行协调者落地。 | `coordination/targets.yaml` 新增 P4-GOV-01 至 P4-REAL-PACKAGE-01；`coordination/integration-board.md` 新增对应状态行；`coordination/path-ownership.yaml` 新增 `real_db/**`、`real_media/**`、`scripts/probe_real_media_key.py`、`scripts/harness_real_readonly.py` 所有权；新增 `coordination/phase-4-waves.yaml`、`coordination/phase-4-dispatch.md`；计划见 `plan/P4-REAL-ROLLOUT-01.md`、`plan/TEMPLATE-result.md`。 | 仅治理与文档变更，不代表任何真实数据读取、解密或媒体预览已通过；P4-REAL-MEDIAKEY-01 的验证结果见后续条目。 |
| F-20260910-011 | 2026-09-10 | P4-REAL-MEDIAKEY-01 通过：真实图片密钥由 cfgDword 确定性派生并通过真实 V2 密文探针校验，一张真实加密图片被解密为可打开的 JPG（487×580，48,918 字节）；35 个源数据库与所选媒体文件的 hash/mtime 前后一致；图片密钥未落盘。上一轮 `ImageKeyNotFound` 的根因确认为使用了密钥优先级中排最末位的进程内存扫描，而非技术不可行。 | 探针 `scripts/probe_real_media_key.py`（新增，只读）；结果 `status=passed`、`cfg_dword_present=true`、`derived_key_validated=true`、`probe_format=V2`、`media_openable=true`、`source_db_unchanged=true`、`source_media_unchanged=true`；私有结果文档 `tests/fixtures_private.local/P4-REAL-MEDIAKEY-01-media-key-probe.local.md`（已忽略）。执行不依赖用户在场，未运行监控式扫描、cleanup 或 executor。 | 不代表真实数据库适配、媒体适配模块、应用内流程、GUI 或打包已完成；密钥提取数量在同会话内出现 19/35 与 27/35 的波动，成功率依赖进程状态；数据库密钥缓存 `keys.json` 由上游写入私有 session（已在私有报告中如实声明），其持久化策略待 P4-REAL-DB-01 决定。 |
| F-20260910-031 | 2026-09-10 | P4-REAL-DB-01 完成：新增可选真实数据库后端 `src/wechat_cleaner/real_db/`（discovery/inventory/adapter），实现真实账号发现（排除 all_users/Backup 等非账号目录）、只读会话与密钥状态盘点（keyed/unkeyed）、匿名库清单与聚合摘要（仅类别名、字节数、计数与 cfgDword 存在性），账号选择支持最大库优先、显式指定与未知账号拒绝。 | `pytest tests/real_db -q` 为 12 passed；全仓 `pytest tests -q`（cwd 为项目根）为 190 passed、5 skipped；`ruff check src tests contracts scripts` 通过；`compileall` 通过；计划与结果文档 `plan/P4-REAL-DB-01-real-db-adapter.md`。测试全部使用合成 db_storage 树与假会话，未接触真实微信数据。 | 不代表真实库的 `PRAGMA quick_check` 批量执行、`DatabaseEvidenceEnvelope` 正式接入或消息/联系人读取已完成（移交 P4-REAL-WORKFLOW-01）；上游私有 `_keys` 映射被适配层依赖（已隔离并固定 revision）；密钥提取数量随进程状态波动，本目标只如实报告未取到类别。 |
| F-20260910-061 | 2026-09-10 | P4-REAL-MEDIA-01 完成：新增可选真实媒体后端 `src/wechat_cleaner/real_media/`（keys/images/locate/adapter），实现 cfgDword 密钥派生与探针校验、v1/v2 容器识别、真实解密经适配层调用、像素与魔数验证、四态媒体可用性（original_available / thumbnail_only / missing / undecodable）、只读定位，以及输出仅落缓存根且路径包含检查。原图缺失时如实降级 `variant=thumbnail` 而非静默返回。 | `pytest tests/real_media -q` 为 32 passed；全仓（项目根为 cwd）`pytest tests -q` 为 178 passed、5 skipped；`ruff check src tests contracts scripts` 通过；`compileall` 通过；结果文档段与 `plan/P4-REAL-MEDIA-01-real-media-adapter.md` 已更新。测试全部使用合成 V2/V1 容器与假下载器，未接触真实微信数据。 | 合成容器通过不等于真实格式全覆盖（真实兼容性由 W1 真机验证与后续私有 harness 背书）；视频/语音/文件定位仅实现只读存在性判定，未做真机覆盖率验证；上游包在项目环境未安装，缺包路径降级为 `DECODER_UNAVAILABLE`（已在测试中覆盖）；不代表应用编排、GUI 或打包已完成。 |
| F-20260910-091 | 2026-09-10 | P4-REAL-WORKFLOW-01 完成：新增应用内真实只读会话 `application/real_workflow.py`，一次调用完成账号发现、真实数据库/媒体适配器装配、匿名库盘点、真实目录只读扫描产出媒体记录、单条预览、会话清单（相对路径）与仅限 `media-preview/` 的局部清理；会话不引用执行器，无删除或移动能力。同时发现并记录跨模块缺口：Phase 2 scanner 按合成约定分类（`msg/<category>/`），真实微信的 `msg/attach/<hash>/<月>/Img` 会被归为 OTHER；本会话以局部 `_refine_media_type` 补丁保留真实媒体，未修改 scanner 目录。 | `pytest tests/application -q` 为 15 passed（含 9 项新增）；全仓 `pytest tests -q`（cwd 为项目根）为 199 passed、5 skipped；`ruff check src tests contracts scripts` 通过；`compileall` 通过；计划与结果文档 `plan/P4-REAL-WORKFLOW-01-real-workflow.md`。测试使用临时合成账号树，真实 scanner 只读运行。 | 不代表真实数据库→联系人映射已完成（记录为 UNMAPPED，需 mapper 接入真实库证据）；不代表 GUI、集成门禁或打包完成；scanner 真实目录分类缺口需其 owner 正式支持后移除本补丁。 |
| F-20260910-131 | 2026-09-10 | P4-REAL-INTEGRATION-01 通过：新增 Phase 4 公开门禁 `scripts/verify_p4.py`（7 项检查，默认不触碰私有输入，报告 `private_data_accessed=false`）、本机真实只读 harness `scripts/harness_real_readonly.py` 与跨模块门禁测试。真实 harness 端到端成功：1 个账号、35 个库 / 1.32 GiB、27 个密钥、103,847 条媒体记录、3 张真实缩略图解密并可打开（media_gate=passed）。诊断并修复两处真实缺陷：原图/高清变体容器不支持时的回退缺失（现回退同名缩略图并如实标注 variant），以及会话清单目录与实际解码缓存布局不一致导致清单漏报。 | `pytest tests/integration/test_p4_gate.py -q` 为 5 passed；`scripts/verify_p4.py --require-passed` 返回 0（7 项全 passed，含全仓 204 passed / 5 skipped、ruff、compileall）；真实 harness 输出 status=passed、preview_openable_count=3、decoded_cache_file_count=3、error_types 为空；私有结果 `tests/fixtures_private.local/P4-REAL-INTEGRATION-01-harness.local.md`。 | 不代表原图与 _h.dat 高清变体可解密（实测为已知限制，签名同为 V2 但密钥/布局不同，v1 仅支持缩略图容器）；8 个库仍无密钥；记录仍为 UNMAPPED（无联系人维度）；GUI 与打包未实现；私有验收未由用户执行。 |
| F-20260910-111 | 2026-09-10 | P4-REAL-GUI-01 完成：新增真实数据只读桌面界面 `gui/real_session.py`（端口协议 + 真实门面 + 假门面）、`gui/real_window.py`（四区窗口：连接向导 / 文件系统维度筛选 / 记录表与预览 / 应用缓存）与 `gui/launcher.py`（入口 + 常见数据根探测）。真实数据端到端冒烟通过：向导检出账号并默认选中最大账号、连接得到 35 库 / 1.3 GB / 27 密钥、加载 103,852 条记录、筛选缩略图 17,801 条、预览成功（缩略图 23.1 KB）、缓存统计 2 文件 / 1 解码、清除解码缓存 1 个且状态栏声明源文件未受影响。窗口不存在任何删除、清理、移动或计划入口（测试逐一断言按钮文本），缓存清理经断言仅作用于应用缓存根。同时修复首轮冒烟暴露的缺陷：向导默认选中列表首项（一个无密钥小号）而非最大账号；并对 0 密钥连接增加醒目提示。 | `pytest tests/gui -q` 为 34 passed（Qt offscreen，含既有 10 项）；全仓 `pytest tests -q` 为 217 passed / 7 skipped（无 Qt 环境自动跳过）；`ruff check src tests contracts scripts` 通过；`compileall` 通过；`scripts/verify_p4.py --require-passed` 返回 0；真实 UI 冒烟输出见私有报告。 | 不代表原图与 _h.dat 高清变体可预览（已知限制，仅缩略图容器支持）；`__main__.py` 挂接未做（归 orchestrator，当前需脚本入口启动）；联系人/时间维度筛选需真实数据库映射；打包未开始。 |
| F-20260910-151 | 2026-09-10 | P4-REAL-PACKAGE-01 打包骨架落地并产出可运行的只读 Beta：新增 `packaging/wechat-space-manager.spec`（PyInstaller one-dir，排除 executor/cleanup 与上游写能力模块）、`packaging/frozen_entry.py`（只导入 GUI launcher，环境变量触发自检）、`packaging/stage_upstream.py`（上游精简暂存）、`packaging/build.py`（暂存 → 构建 → 校验 → 清单，输出到带构建 ID 的独立目录）、`packaging/verify_artifact.py`（拉起冻结 exe 做启动自检）、`packaging/licenses/`（Apache-2.0 全文 + 第三方声明）与 `tests/packaging/test_build_policy.py`。构建产物约 214.6 MB / 726 个模块，`BUILD-MANIFEST.json` 记录源码与上游 revision。 | `packaging/build.py` 返回 0（PYZ 模块数 726、许可证齐全、无隐私命中、体积 214.6 MB）；`pytest tests/packaging -q` 为 12 passed；冻结 exe 在 `WCSM_SELF_TEST=1 QT_QPA_PLATFORM=offscreen` 下启动成功并输出窗口标题「微信空间管理器（只读预览）」与按钮清单（无任何删除/清理源文件入口）。 | 不代表无 Python 干净环境已实测（属人工验收）；未做代码签名、安装器或自动更新；产物体积主要来自 PySide6 与 cryptography；原图与 _h.dat 仍不可解密（继承 P4-REAL-MEDIA-01 未完成项）。 |
| F-20260910-152 | 2026-09-10 | 打包暴露并修复两处真实缺陷。① 上游 `wechatauto` 为 editable 安装，PyInstaller 静态分析无法跟随 PEP 660 finder，首轮产物仅收集到 4 个资源文件、Python 代码完全缺失（若交付，用户点击「连接」必然 ModuleNotFoundError；首轮 130 MB 的体积偏小实为模块缺失所致）。② 排除 cleanup 后冻结产物启动崩溃：`ModuleNotFoundError: No module named 'wechat_cleaner.cleanup'`，根因是 `application/__init__.py` 与 `gui/__init__.py` 顶层导入合成路径 facade，而 `workflow.py` / `services.py` / `window.py` 顶层硬导入 cleanup。 | 修复一：`packaging/stage_upstream.py` 生成仅含 `db.py` / `media.py` 与极简 `__init__` 的精简副本，由 spec 的 `pathex` 优先解析；复检 PYZ 中 `wechatauto` 恰为 3 个模块（包本体 + db + media），而 `wx` / `guia` / `moment` / `sender` / `uia_driver` / `msgs` / `ui` / `uia` / `utils` 全部为 0。修复二：两处 `__init__.py` 改为 PEP 562 懒加载，只读符号静态导入、合成 facade 符号首次访问才解析；验证导入 `gui` 与 `real_session` 后 cleanup / executor 均未加载，访问 `ApplicationFacadeAdapter` 时才拉起；`pytest tests/application tests/gui -q` 为 26 passed、5 skipped（缺 PySide6 环境）。 | 未修改上游任何源码（故第三方声明记为无修改）；未把上游源码复制进仓库（仅暂存于已忽略的 `build/`）；懒加载属跨模块改动，文件为 `src/wechat_cleaner/application/__init__.py`（owner application_workflow）与 `src/wechat_cleaner/gui/__init__.py`（owner gui_shell），建议由对应 owner 复核。 |
| F-20260911-001 | 2026-09-11 | 诊断并修复打包版"只看到少量照片"的根因：真实账号含 100k+ 媒体文件，GUI 把全部约 104k 条记录一次性渲染进非虚拟化的 QTableWidget，导致窗口冻结/局部绘制，用户误以为只索引到少量照片；同时预览仅支持 `_t.dat` 缩略图（原图与 `_h.dat` 不可解，已知限制，继承 P4-REAL-MEDIA-01）。已对 `gui/real_window.py` 做三处改造：渲染行数上限 `_MAX_DISPLAY_ROWS=2000`、按 `observed_at` 倒序、可用性惰性计算（仅对可见行/激活筛选调用 availability）。 | 对真实主账号（账号标识已隐去）运行 scanner + `_refine_media_type` 只读统计：总文件 115,261，精炼后为 image 76,438 / thumbnail 17,823 / video 6,177 / file 3,457（约 103,895 条媒体记录），与 F-20260910-131 的 103,847 吻合，证明索引完整而非缺失；`py_compile` 通过，28 个相关测试通过。 | 本会话构建 venv 当前缺失 PySide6/PyInstaller（pip 缓存清理被安全钩子拦截），未能在本机重建冻结包；用户需在其构建环境运行 `python packaging/build.py` 方可获得带修复的可执行文件。不代表面向 104k 行的筛选/排序性能已全量验收；原图与 `_h.dat` 预览仍为已知限制。 |
| F-20260911-002 | 2026-09-10→11 | 带 GUI 渲染修复的只读 Beta 冻结包重建成功并通过自检。使用 `real-readonly-venv-isolated-20260909`（含 wechatauto + PySide6 6.11.2 + PyInstaller 6.22.2，wechatauto 指向本地 clone）并以 PYTHONPATH 挂项目 src 完成构建；项目 .venv 当前被安全钩子拦截 pip 文件操作，无法直接安装 wechatauto，故改用既有 real venv。 | 产物 `dist/wechat-space-manager-20260911T020151Z`（214.6 MB / 719 模块）；`verify` 通过（许可证齐全、无 cleanup 路径、无上游写能力模块、无隐私命中）；冻结 exe 在 `WCSM_SELF_TEST=1 QT_QPA_PLATFORM=offscreen` 下启动成功，输出标题「微信空间管理器（只读预览）」与 9 个按钮，无任何删除/清理入口。 | 仍未做代码签名/安装器/自动更新；未做无 Python 干净环境人工验收；原图与 `_h.dat` 仍不可预览（继承已知限制）。 |
| F-20260911-003 | 2026-09-11 | 修复用户实测"按可用性筛选后软件假死"：F-20260911-001 的可用性懒加载在 availability 筛选激活时退化为对全部匹配记录（约 10 万条）逐条 `Path.resolve()` + 双 stat，全在 UI 线程执行导致窗口冻结。三处改造：① `real_media/locate.py` 的 `resolve_source_path` 改纯字符串归一化（`normpath`+`normcase` 前缀检查防逃逸，去掉每次触盘的 `Path.resolve()`；scanner 本就拒绝 reparse point，无需符号链接展开）；② `locate()` 合并为单次 `os.stat`（原来 `is_file()` + `stat()` 两次）；③ `gui/real_window.py` 可用性筛选改为分批处理，每 1000 条刷新状态栏进度"正在检查源文件可用性 N/M"并 `processEvents`，期间禁用 5 个按钮防重入，捕获 `FacadeError` 中止保护。同时澄清用户误读：连接摘要"35 库 / 1.3 GB"仅指数据库（聊天索引），媒体本体约 84GB 体现在记录列表（约 10.4 万条）。 | 隔离 venv（pytest 9.1.1 + PySide6 6.11.2）`pytest tests -q` 为 252 passed、2 failed（均为 Windows symlink 能力测试，与本次改动无关，原门禁 venv 中通过）；`ruff check src tests contracts scripts` 通过；`compileall` 通过；真实路径只读基准 3000 条可用性检查 0.47s（约 6,350 条/s），全量 10.4 万条估计约 16 秒；带修复的冻结包 `dist/wechat-space-manager-20260911T025049Z`（214.6 MB / 719 模块）verify 通过，冻结 exe 自检 9 按钮无删除入口。 | `locate.py` 属跨模块改动（owner real_media），建议 owner 复核防逃逸语义等价性；可用性全量检查约 16 秒仍需 UI 等待（有进度提示，未做后台线程）；原图与 `_h.dat` 预览仍为已知限制。 |
| F-20260911-004 | 2026-09-11 | 修复用户实测"点击连接即卡死"：连接在 UI 线程同步执行上游 `WeChatDB` 会话（密钥提取 + 解密 1.32 GiB 数据库进缓存，且 adapter 与 media backend 各开一次会话），随后 `load_records` 又同步全盘扫描 11.5 万文件，均为秒到分钟级阻塞。改造 `gui/real_window.py`：新增 `_FacadeWorker(QThread)` 把阻塞调用移出 UI 线程，窗口用局部 `QEventLoop` 等待（调用方控制流保持同步，测试无需改动），等待期间状态栏显示"正在连接（提取数据库密钥并解密数据库…）"/"正在扫描媒体文件…"、全部 8 个按钮禁用防重入，`closeEvent` 在忙碌时拒绝关闭（防止 worker 使用中的 facade 被销毁）；worker 捕获 `BaseException`（含安全钩子可能抛出的 `SystemExit`）保证等待循环必然退出。 | 隔离 venv（PySide6 6.11.2）`pytest tests -q` 为 253 passed、1 skipped（symlink 能力跳过）；`ruff check` 通过（含 import 排序自动修复）；`compileall` 通过；冻结包 `dist/wechat-space-manager-20260911T034721Z`（214.6 MB / 719 模块）verify 通过，冻结 exe 自检 9 按钮无删除入口。 | 连接耗时本身未减少（上游管线照旧，两次会话可否合并为一次待优化），只是不再冻结界面；扫描仍无逐条进度（单次阻塞调用，仅状态栏提示）；忙碌期间用户无法关闭窗口属有意行为。 |
| F-20260911-005 | 2026-09-11 | 应用户四项需求改造并**破解原图容器**。探针 `scripts/probe_original_container.py`（只读、密钥仅内存）证实四类容器布局：`_t_W.dat`（41,229 个）为 XOR-only JPEG（payload ^ xor_key，偏移 0）；真原图 `<32hex>.dat`（9,805 个）与 `_h.dat`（3,764 个）为 V2 头 + AES-ECB（载荷偏移 15、PKCS#7 填充）——上游此前解不开原图是因其未支持该偏移，本会话自研解码成功（实测解出 1080×2400 JPEG）。据此：① `real_media/images.py` 新增 `self_decode_file`（XOR translate 表扫描 + AES 偏移扫描，>4MiB 大文件只解前缀，截断渲染出预览），`decode_image_file` 在上游失败后自动兜底，PKCS#7 仅对完整载荷剥离；② `application/real_workflow.py` 将 `_t_w.dat` 归入 THUMBNAIL 家族，预览缓存上限 512MB→2GB；③ GUI：表头点击排序（大小列按真实字节数、时间列按时间戳，再次点击切换升降序；特意不用 `setSortingEnabled(True)` 因其会立即按第 0 列重排打乱插入顺序），选中行经 item 的 media_id 映射（排序后位置选择不串行）；时间档位筛选（全部/最近 7/30/90 天/最近一年/自定义日期之前，QDateEdit）；选中行 300ms 防抖自动预览。 | 隔离 venv `pytest tests -q` 为 260 passed、2 failed（均为已知 Windows symlink 环境性失败，与改动无关）；新增 8 项测试（XOR/AES-15 自解码 3 项、变体分类 1 项、GUI 排序/排序后选中/日期筛选/自动预览 4 项）；`ruff check` 与 `compileall` 通过；真实数据探针证据见探针输出（XOR 偏移 0 解出 150×100 JPEG、AES 偏移 15 解出 PNG/JPEG）；冻结包 `dist/wechat-space-manager-20260911T042441Z`（214.6 MB / 719 模块）verify 通过，冻结 exe 自检 9 按钮无删除入口。 | `Img` 目录下 `_W.dat`（20,550 个）未证实可解（探针样本失败，可能混合视频变体），这些记录预览仍可能失败；>4MiB 原图预览为顶部部分渲染（如实降级）；`verify_pixels`/`_load_for_output` 全局启用 `LOAD_TRUNCATED_IMAGES`（本应用进程内可接受）；记录仍为 UNMAPPED；无删除能力不变。 |
| F-20260911-006 | 2026-09-11 | 修复用户实测"部分'原图可用'记录预览报 DECODER_UNAVAILABLE: media_type file is not decodable"：根因不是图片未破解，而是筛选语义混类——`msg/file` 下的文件、语音、视频的"源文件在盘"也被 `availability()` 标为 `original_available`，混进"原图可用"结果，选中后命中 `RealMediaBackend.decode_image` 的类型门。两处修复（均在 `gui/real_window.py`）：① 可用性筛选为 `original_available`/`thumbnail_only` 时按类型收窄到 IMAGE/THUMBNAIL（还省去非图片记录的 stat；`missing`/`undecodable` 保持全类型）；② `request_preview` 对非图片记录前置拦截，返回中文可读原因"该记录不是图片（视频/文件/语音暂不支持预览）"而非后端英文报错。 | 隔离 venv `pytest tests -q` 为 263 passed、1 skipped；`ruff check` 通过；新增 2 项 GUI 测试（原图筛选排除非图片、非图片预览可读原因）；冻结包 `dist/wechat-space-manager-20260911T060906Z`（214.6 MB / 719 模块）verify 通过，冻结 exe 自检 9 按钮无删除入口。 | `availability()` 后端语义未动（文件"在盘"仍标 original_available），收窄只在 GUI 筛选层；视频/文件/语音依旧无预览能力（文件非图片无从解码；视频首帧预览列为后续可选项）。 |
| F-20260911-007 | 2026-09-11 | UI 性能优化三件套，消除"随机选什么都会卡一下/等好久"：① 根因一，每次预览都全量扫缓存——`decode_image` 每调一次 `purge_expired` + `usage_bytes`（os.walk 整个 2GB 缓存、逐文件 stat，数千文件每次数百 ms）；`real_media/adapter.py` 改为 30 秒节流维护（`_maybe_maintain_cache`），间隔内用增量计数估算用量，接近上限时刷新一次真实值再判，避免陈旧计数误拒。② 根因二，预览解码在 UI 线程——`real_window.py` 的 `request_preview` 改经 `_run_blocking`（QThread + 局部 QEventLoop）后台解码，状态栏显示"正在解码预览"；busy 结束后若用户已换行自动补触发；`_last_previewed_id` 去重防循环（同一行本会话只自动预览一次，手动点按钮不受限）。③ 原图族省掉上游双跳——`decode_image_file` 仅对 `_t.dat` 家族先试上游（其唯一支持的容器），其余直接走自研解码，避免"先把整个大文件 AES 解一遍再失败"。 | 隔离 venv `pytest tests -q` 为 262 passed、2 failed（已知 Windows symlink 环境性失败，本轮改动无关）；`ruff check` 与 `compileall` 通过；冻结包 `dist/wechat-space-manager-20260911T063439Z`（208.1 MB / 719 模块）verify 通过，冻结 exe 自检 9 按钮无删除入口。 | 本机 Git Bash PATH 损坏（coreutils 丢失），构建命令临时用 PATH 前缀绕过，未影响产物；usage 增量计数在跨会话残留缓存时首刷即校准；预览期间全部按钮仍禁用 1-3 秒（后台化后 UI 可交互但按钮锁定，属有意防重入）。 |
| F-20260911-008 | 2026-09-11 | 整体体验审视落地四项。① **连接提速约一半**：`RealReadOnlySession.start` 生产路径合并为单次上游会话——原实现 adapter 与 media backend 各开一次 `WeChatDB`（密钥提取+整库解密跑两遍）；现在 `real_media/adapter.py::RealMediaBackend.open` 接受注入 `session`，`real_workflow` 用 `open_source_session` 开一次后经 `session_factory` 注入共享。② **数据根自动探测修正**：`launcher.discover_default_data_root` 不再"第一个命中就用"（首候选恰是 Documents 下 77MB 旧实验副本，曾误导用户），改为逐候选真实发现并按账号库字节总量选优。③ **心理感知**：所有后台步骤状态栏每秒刷新"（已 N 秒）"（QTimer busy 时钟）；可用性检查进度带百分比；扫描完成显示实际用时、下次提示"上次用时 X 秒"。④ **渲染微优化**：`_render_table` 包 `setUpdatesEnabled(False/True)`。 | 隔离 venv `pytest tests -q` 为 263 passed、2 failed（已知 symlink 环境性 flaky，与改动无关）；新增"多候选选大"探测测试；`ruff check` 与 `compileall` 通过；冻结包 `dist/wechat-space-manager-20260911T071424Z`（208.1 MB / 719 模块）verify 通过，冻结 exe 自检 9 按钮无删除入口。 | 共享会话双 close（adapter 与 downloader.db 各调一次）依赖两处 close 的异常吞噬兜底，上游 close 幂等性未实测；扫描为单次阻塞调用仍无逐文件进度（已有时长提示）；上游管线内部进度不可观测（第三方黑盒）。 |
| F-20260911-009 | 2026-09-11 | 用户实测"很多图预览不可用（DECODE_FAILED: decrypted payload is not a valid image）"，对截图定位的 12.1MB `_h.dat` 深度探针（JPEG marker-walk + 跨尺寸采样 + 头部字段对比）确认：**大 `_h`/原图仅"头部段"（SOI/APP0/APP1/EXIF，约 3.8KB）AES-ECB 可解，EXIF 之后的熵编码主体是未破解结构**（CBC-zero-IV、per-chunk IV+CBC(4096/65536)、key2=MD5(k) 假说均被数据否定；坏点位置与块边界无整除关系）。同时确认 **`Img` 下大 `_W.dat`（1~12MB，XOR 全文件）可完整解码**（实测解出 6240×4160 JPEG，即真原图家族）。据此落地两级降级：① `images.py` 新增 `_embedded_image`——大文件解密前缀整体不可解码时，搜索其中 EXIF 内嵌的完整 JPEG（位于可解的头部区内）作为预览；② `locate.py::thumbnail_sibling` 回退链扩展 `_t.dat` → `_t_W.dat`（XOR 族，4.1 万张）。 | 隔离 venv `pytest tests -q` 为 267 passed、2 failed（已知 symlink 环境性 flaky）；`ruff check` 与 `compileall` 通过；冻结包 `dist/wechat-space-manager-20260911T080925Z`（208.1 MB）verify 通过，冻结 exe 自检 9 按钮无删除入口；探针证据：`_W` XOR 全解 6240×4160、大 `_h` walker 断点=EXIF 结束（20+2+3757）、CBC/分块假说实测否定。 | 大 `_h`/大原图的熵编码主体加密结构仍未破解（预览为 EXIF 内嵌小图或 `_t_W` 缩略图，非原图本体）；`_embedded_image` 对每个 SOI 候选做 Pillow verify+load，最坏多次尝试（前缀 4MB 内，实测可接受）；`.tmp` 探针脚本已清理。 |
| F-20260911-010 | 2026-09-11 | **V2 容器结构完全破解，"很多大图预览不可用"根因修复**。对照上游源码 `wechatauto/media.py::_decrypt_v2` 确认 V2 容器为三段式：15B 头（magic 6B + aes_size LE32 + xor_size LE32 + 1B）后依次为 ① AES-ECB(imageKey) 加密的 `aes_size` 字节（对齐 16，即 JPEG 头部段，通常仅 KB 级）② **明文图像主体** ③ 尾部 `xor_size` 字节 XOR（imageKey 的 xorKey）。此前"大图主体解不开"系两重误判：一是 self-decode 把"明文主体"继续当 AES 密文解（拼出垃圾），二是"上游对大图会失败"的判断错误——上游 `_decrypt_v2` 本就正确，且 AES 段仅 KB 级、三段拼接远快于旧的全文件解密。修复：`real_media/images.py` 重写 `_self_decode_v2`（按头部声明分段：AES 去填充 + 主体透传 + XOR 尾 translate），`decode_image_file` 恢复 **V1/V2 上游优先**（UNKNOWN XOR 族仍走 self-decode）；EXIF 内嵌图与 `_t_W` 兄弟两级降级保留为兜底。真实文件验证：12.7MB `_h` 在 **关闭截断容错的金标准**下干净解出 **7906×4914 JPEG**；跨尺寸复验 2.26MB（实为 PNG，1058×6055）、3.85MB（3207×2160）、6.57MB（2160×2880）原图全部完整解码——此前"仅头部可解"的结论系旧逻辑花屏/误报。 | 隔离 venv `pytest tests -q` 为 267 passed、2 failed（已知 symlink 环境性 flaky）；`ruff check` 与 `compileall` 通过；合成三段式夹具测试通过（dat_factory 的 aes_size=全长/xor_size=0 为三段式退化特例，天然兼容）；冻结包 `dist/wechat-space-manager-20260911T083303Z`（208.1 MB）verify 通过，冻结 exe 自检 9 按钮无删除入口。 | 金标准验证仅覆盖采样文件；>64MB 文件读取上限截断（`_SELF_DECODE_MAX_BYTES`，超出部分预览缺尾部，属边缘）；`load_records`/`availability` 与本修复无关的旧结论（如"_h 仅头部可解"）已在本文与 fact-log 修正，需以本轮为准。 |
| F-20260911-011 | 2026-09-11 | 三项用户反馈落地。① 残留 `OSError` 失败诊断+修复：320 个 10MB+ 大文件批量探针 0 失败（用户所见应属旧版包）；根因锁定为"头部完好但主体截断/损坏时 `verify()` 通过而 `load()` 抛 OSError"——`_usable_image_or_embedded` 可用性判定升级为**完整 load()**，此类文件自动降级 EXIF 内嵌图；`_SELF_DECODE_MAX_BYTES` 4MB→**256MB**（50MB+ 原图不再截断）。② **回收站删除功能上线**：新增 `real_media/trash.py`（Windows Shell `SHFileOperationW` + `FOF_ALLOWUNDO`，仅回收站、无永久删除路径；TOCTOU 身份复核 size+mtime_ns 不符即拒 `FILE_IDENTITY_MISMATCH`；成功追加 `recycle-receipts.jsonl` 审计收据）；`real_session.py` 端口/facade/fake 增 `recycle_record`→`RecycleOutcome`；`real_window.py` 预览区新增「移到回收站…」按钮（单文件、QMessageBox 显式确认、后台线程执行、成功后从记录集移除）。只读保证测试语义同步更新：禁止"永久/彻底/直接删除"，唯一移除能力=确认后的回收站移动。③ 合并聊天记录关联（仅规划未实现）：链路=解 keyed message.db → type=49 合并转发 XML 提取子消息（svrid/标题/cdn）→ 与 msg/attach 按 hash 目录+时间窗+大小启发式关联 → UI 增"来源会话"列/筛选。 | 隔离 venv `pytest tests -q` 为 273 passed、2 failed（已知 symlink flaky）；新增 trash 3 项（成功收据/身份漂移拒绝/文件缺失拒绝）+ 真实 Shell 回收站集成测试 1 项（Windows 实测通过）+ GUI 确认/拒绝 2 项 + 只读语义测试更新；`ruff check` 与 `compileall` 通过；冻结包 `dist/wechat-space-manager-20260911T090348Z`（208.1 MB / 720 模块）verify 通过，冻结 exe 自检 10 按钮含「移到回收站…」、无永久删除入口。 | **Agent 未执行任何真实删除**：真实 Shell 集成测试仅移动一个合成 tmp 文件；回收站操作永远由用户在 UI 逐条触发；批量删除仍不存在。合并聊天记录关联属规划（依赖 keyed db 映射，见 F-20260910-091 的 UNMAPPED 说明）。 |
| F-20260911-012 | 2026-09-11 | 用户报告三个不可预览文件，只读探针全部定位并修复。① **109MB 设计 PDF**（`Rec\77b8511b…\F\3\2022-11-25温岭新河EPC项目（一期）设计方案（.pdf`）：scanner 的 Rec 规则把 Rec 下所有非 `_t` 文件一律判 IMAGE，而 Rec 实为**合并转发聊天记录的混合附件区**（实测同目录还有 5 个 PDF，38KB–109MB）。修复：`real_workflow._refine_media_type` 增加 Rec 区扩展名修正表（.pdf/.doc/.mp4/.amr 等 → FILE/VIDEO/VOICE），`.dat` 与无扩展名保持原判；修正后该 PDF 归入"文件"筛选，预览点击给既有中文提示"该记录不是图片"。② **两个 99,606,414B 的 `_W.dat`**（7efc950e/57fb69ff 两目录，md5 相同=同一文件两份）：XOR@0 用 xor_key 完整命中 `\x89PNG`——是完好无损的 **11225×16006（1.797 亿像素）拼接长图**，超过 Pillow `MAX_IMAGE_PIXELS` 硬上限 178,956,970 仅 0.4%，`Image.open` 抛 `DecompressionBombError` 被误判为"不是有效图片"。修复：`images._relax_pillow_bomb_guard()`（`MAX_IMAGE_PIXELS=None`，依据：输入为本地受信文件且已被 `_SELF_DECODE_MAX_BYTES`=256MB 封顶）应用于 `verify_pixels`/`_usable_image_or_embedded`/`_load_for_output`；`_load_for_output` 对非 JPEG 增加 `thumbnail(2048)`（PNG 无 draft()，否则 1.8 亿像素全量重编码进缓存）。端到端实测：99MB 源 → 解码 0.3s → 验证 1.5s → 预览 1436×2048 缓存 PNG 2.5MB（共 4.3s）。新增探针脚本 `scripts/probe_reported_unpreviewable.py`（存在性/分类/头部/密钥命中/md5 去重）。 | 隔离 venv `pytest tests -q` 277 passed、2 failed（已知 symlink 环境性 flaky，新增 4 测试：bomb 放宽 1 + 非 JPEG 降采样 2 + Rec 分类 1，全走 scanner→refine 真实管线）；`ruff check` 与 `compileall` 通过。 | 炸弹保护仅对本工具的本地受信输入放宽，不对网络输入生效；1.8 亿像素全解码瞬时内存约 0.7GB（worker 线程，UI 不卡）；Rec 区仅按白名单扩展名修正，未列出的扩展名（如 .heic）仍按目录规则。 |
| F-20260911-013 | 2026-09-11 | 导出功能上线：「导出原图…」「导出后移入回收站…」。① `real_media/images.py` 新增 `decode_image_bytes`（`decode_image_file` 改薄壳），返回 `(payload, container, is_original)` 三元组——`_usable_image_or_embedded` 以"完整 load 成功"判定原图本体，源主体损坏时降级 EXIF 内嵌图并如实标 `is_original=False`；② `real_media/adapter.py::export_original`：类型门（IMAGE/THUMBNAIL）→ 可用性门（MISSING/UNDECODABLE 拒绝）→ `decode_image_bytes`+`verify_pixels`，**无缩略图回退**（拿替代品冒充原图不诚实），失败抛 `DecodeError`；③ `gui/real_session.py` 新增 `ExportOutcome` + 端口/真实门面/假门面；④ `gui/real_window.py` 预览区新增两按钮：QFileDialog 选目标（默认 Pictures、记忆上次目录、默认名去 `_W`/`_t` 容器后缀）→ worker 线程内解密+写盘（实际格式与 .jpg 不符时 `with_suffix` 修正）→ 状态栏报告；导出后回收站=显式确认（默认 No）→ 文件框 → worker 内 export→recycle 顺序执行（复用 F-20260911-011 Shell 回收站链路），回收站失败不回滚导出、如实报告；⑤ 附带修复：`_set_busy` 恢复按选中记录恢复选择类按钮、`closeEvent` 停预览定时器 + `_window_alive()` 防 Qt 对象销毁竞态；`verify_artifact.py` 策略更新（「回收站」不再是禁语——受控单文件操作，「移到回收站…」「导出原图…」「导出后移入回收站…」加入白名单）。 | 隔离 venv 全量 `pytest tests -q` 为 **294 passed、1 skipped**（symlink 能力跳过；新增 15 项测试：images 3、adapter 6、Rec 分类 1、real_session 3、real_window 8）；`ruff check` 与 `compileall` 通过；真实端到端 `scripts/verify_export_e2e.py`：99,606,414B `_W.dat`（1.797 亿像素 PNG）**4.3s** 导出完整 PNG（head=`\x89PNG`，99,606,414B，11225×16006，`is_original=True`），建议名正确去 `_W` 后缀；冻结包 `dist/wechat-space-manager-20260911T111121Z`（214.7 MB / 727 模块）verify 通过，冻结 exe 自检 12 按钮含两导出入口、无删除/清理入口，`packaging` 测试 12 passed。 | 导出为解密后**原始字节**（非预览重编码）；`is_original=False` 时状态栏标注"源文件主体不完整，导出的是其内嵌缩略图"；回收站删除仍由用户逐条显式确认触发、无批量能力；>256MB 文件超 `_SELF_DECODE_MAX_BYTES` 仍拒绝（边缘）；回收站失败不回滚导出（导出保留）。 |
| F-20260911-014 | 2026-09-11 | 应用户需求加进度反馈：① 状态栏右侧新增 QProgressBar——黑盒步骤（连接=密钥提取+解密库、预览解码、导出、缓存清除）用不确定脉冲模式（range 0,0，诚实表达"时长不可观测"），有真实分母的步骤画真进度；② **扫描真进度**：`scanner.scan_account` 新增可选 `progress` 回调（每 512 个文件上报一次已发现文件数，`_walk_account` 纯增量参数，默认 None 行为不变），`real_workflow.records(progress=)` 与 facade/port/fake 全链路转发；GUI 侧 `_FacadeWorker` 新增 `progress = Signal(int)`，worker 线程回调经 `_emit_progress` 桥接为队列信号，UI 线程槽更新状态栏"已扫描 N 个文件"（busy 时钟每秒刷新时保留该片段）；③ **可用性检查真百分比上屏**：`_resolve_availability` 把已有的真实百分比分批驱动到进度条（0% 起步、每批 setValue、结束隐藏）。 | 隔离 venv 全量 `pytest tests -q` 为 **301 passed、1 skipped**（symlink 能力跳过；新增 7 项测试：scanner 回调节奏/不变性 1、workflow 转发逐文件计数 1、fake facade progress 1、GUI 脉冲显示隐藏/空闲 no-op/扫描计数上屏/可用性真百分比 4）；`ruff check` 与 `compileall` 通过；冻结包构建+verify 通过，冻结 exe 自检通过。 | scanner.py 为跨模块增量改动（owner scanner），建议复核：progress 仅是只读通知、不影响扫描语义与 manifest digest；连接/解码阶段的上游管线仍不可观测（脉冲条+秒数计时是诚实上限）；进度信号队列化（每 512 文件一次，10 万文件约 200 次信号）对事件循环开销可忽略。 |
| F-20260911-015 | 2026-09-11 | 会话筛选与合并转发筛选上线（应用户定向筛选照片需求）。**关键结构发现**（探针 `scripts/probe_session_mapping.py`，真实主账号只读）：`msg/attach/<hash>` 的 hash = `md5(会话 username)`，747/752 目录精确匹配会话（99.3%），md5(username) 抽样验证 200/200——会话归属是精确可逆的目录属性，无需启发式。据此实现：① `real_db/mapping.py`：`session_dir_of`（路径→attach 目录名）、`is_merged_forward`（任意路径段==Rec，大小写不敏感；Rec 为微信合并转发专用目录）、`build_session_mapping`（contact/session 库反查索引 `_build_md5_index`+`_nickname_index`，轻量路径 0.72s 解析 748/752 目录、250 个群聊；失败降级 `list_message_chats`，再失败按 hash 前缀兜底）；② `RealReadOnlySession` 保存共享 upstream 会话并新增 `session_mapping()`（advisory，异常返空）；③ facade/port/fake 三层 `session_mapping`；④ GUI 筛选区第二行：会话下拉（editable+MatchContains 补全，按媒体数排序，"小张［联系人］（1234 项）"格式）+「仅合并转发记录」复选框，与类型/可用性/大小/时间并联（AND），会话收窄在 availability 检查前执行省 stat；扫描时同一 worker 调用内顺带取 mapping。 | 隔离 venv 全量 `pytest tests -q` 为 **313 passed、2 failed（已知 symlink 环境性 flaky，与本改动无关）**；`ruff check` 与 `compileall` 通过；真实端到端 `scripts/verify_session_mapping_e2e.py`：104,013 条记录、94,364 条归属到 752 个会话目录、9,649 条（msg/file、msg/video、msg/voice 按月布局）如实标注不可归属、合并转发记录 1,281 条分布 70 个会话；新增 13 项测试（mapping 6 + workflow 3 + GUI 3 + fake facade 1）；冻结包构建+verify 通过、冻结 exe 自检通过（自检白名单同步加入「仅合并转发记录」——纯只读筛选复选框）。 | 会话筛选仅覆盖 `msg/attach` 布局（图片/缩略图/高清变体/Rec 附件）；文件、视频、语音按月存放无会话属性，会话筛选激活时被排除（GUI 状态栏有注明）；文件级精确关联不做（探针 image md5→文件存在率 52%，会话级归属不受影响）；`Rec/<record-id>` 与具体消息的对应未破解（对照 server_id hex 0/12），"合并转发"筛选按目录段判定已满足需求；mapping.py 依赖上游私有 `_build_md5_index`/`_nickname_index`（固定 revision 1.2.1，有 F-20260910-031 先例）；显示名只在 UI 内存，不落盘。 |
| F-20260911-016 | 2026-09-11 | 预览体验升级：后台批量预热（应用户"像浏览文件夹一样，不要点一张加载一张"需求），并**修复隐藏至今的预览缓存永不命中缺陷**。① **缓存命中缺陷**（预热验证脚本暴露）：`RealMediaBackend.decode_image` 只有写缓存、从不读回——每次预览都全量解密+重编码，此前被 `_last_previewed_id` 去重（同一行本会话只自动预览一次）掩盖；真实实测冷 1.1s/次、重访同样 1.1s。修复：`_cache_hit` 以 `(cache_session_id, media_id, variant, max_edge_px)` 为键读缓存（edge 进键防 256px 满足 512px 请求；旧无后缀文件名作为兜底候选读取，既有缓存不失效），命中返回 `DecodeArtifact`（created_at=缓存文件 mtime）；写路径文件名加 `-e{edge}` 后缀；实测重访 **1.1s → <0.001s**。② **后台预热**：`gui/real_window.py` 新增 `_WarmRunnable(QRunnable)` + 2 线程 `QThreadPool`——渲染后自动预热显示顺序前 60 行；选中行时其前后各 20 行优先入队；预热项经 `_last_previewed_id`/`_warmed` 去重；前台 busy 期间预热让路（轮询等待至 30s）；完成回调仅做 GIL 原子计数维护，无 Qt UI 访问；`closeEvent` 置停止标志 + `pool.clear()`，在飞的预热循环检测标志自行退出。 | 隔离 venv 全量 `pytest tests -q` 为 **320 passed、2 failed（已知 symlink 环境性 flaky，与改动无关）**；`ruff check` 与 `compileall` 通过；新增 7 项测试（adapter 缓存命中/edge 键/会话隔离/旧名兜底 4 + GUI 预热批次/邻域优先/关闭停止 3）；真实端到端 `scripts/verify_preview_warming_e2e.py`：12 张混合批次冷 5.99s → 热 <0.001s；冻结包构建+verify 通过、冻结 exe 自检通过。 | 预热受缓存上限约束（2GB，满了会拒新预热并如实报错）；预热只解缩略图变体（跟随当前下拉选择）；预热项数量与可见行相关（每渲染 60 行 + 选中邻域 40 行），不做全库 10 万张预热（无意义且爆缓存）；多线程共享 backend 的维护计数在无锁下最坏偏差一次节流判断（GIL 原子读写，无数据损坏）；2 线程并行解码中 PIL/AES 释放 GIL，实际并行有效。 |
| F-20260911-017 | 2026-09-11 | 预览方式改为**手机相册式平铺网格 + 多选批量管理**（应用户"不要点一张加载一张，所有预览图平铺在界面上，可多选管理"需求）。① **网格视图**（新模块 `gui/gallery.py`）：`MediaGridModel(QAbstractListModel)` 承载筛选后的全部记录——Qt item view 自带虚拟化，**去掉了 `_MAX_DISPLAY_ROWS=2000` 的显示上限**，10 万条记录不再需要截断；`MediaGalleryView(QListView)` 配置 `IconMode + wrapping + uniformItemSizes + ExtendedSelection`，单元格 = 132px 缩略图 + 两行说明（类型字形/文件名前缀、大小/日期）。② **懒加载解码**：只对可视区域（`indexAt` 两个角点求 first/last）+ 前后各 120 格缓冲入队解码 256px 缩略图，滚动/缩放的 `scrollContentsBy`/`resizeEvent` 继续排队——滚到哪解到哪，不设硬上限；快速滚动时 `_order_warm_queue_by_visibility` 把可视区内未解码的格子顶到队首；解码线程池由 2 线程提到 **4 线程**（PIL/AES 释放 GIL）；解码失败记入 `_warm_failed` 不再重试（视频无静帧即此路径）。③ **多选与批量管理**：`selected_records` 属性 + `select_rows`/`select_all_cells`；单选动作（预览/导出/回收站）要求恰好选中 1 项，批量动作（批量导出原图/批量移到回收站/批量导出后移入回收站）在选中 ≥1 项时可用；导出的批量动作跳过非图片并如实标注（`skipped`）；「导出后移入回收站」严格逐文件串行——导出成功才回收该文件，导出失败的源文件**保持不动**；重名原图在同目录自动加 `（n）` 后缀防覆盖；新增 `BatchOutcome` 聚合结果（逐文件 success/fail/skip + 汇总）。④ 状态栏改为报批量结果（不再被筛选统计覆盖），选中数量与总字节实时显示。 | 隔离 venv 全量 `pytest tests -q` 为 **341 passed、1 skipped**（symlink 环境性跳过）；`ruff check` 通过；新增 19 项测试（`tests/gui/test_gallery.py` 12 项：模型数据角色/异步缩略图/失败记录/可见范围/信号，`tests/gui/test_real_window.py` 多选与批量 7 项：多选亮按钮/多选不自动预览/批量导出/跳过非图片/批量回收/取消不动/逐文件失败不中断/导出后回收保留未导出项）；`packaging/verify_artifact.py` 白名单加入三个批量按钮。 | 网格不再提供表头列排序（相册无列排序语义；顺序固定为时间倒序，收窄请用筛选控件）；批量回收仍是**对微信源文件的写操作**，一次确认作用于 N 个文件，逐文件写审计收据，但确认次数从 N 次降为 1 次；批量导出为每文件各自解密，失败不中断其余项；`_warm_failed` 里的视频不会被静帧预览（本阶段不做视频首帧）；缩略图缓存键含 256px edge，与详情页 256px 预览共享缓存条目。 |
| F-20260911-018 | 2026-09-11 | **真实 Windows 平台静默退出根因定位并修复**（承 F-20260911-017；此前被误判为「`show()` 之后进程静默退出」，实为 access violation）。① **根因**：上游 `MediaDownloader`（内含单个 SQLCipher 连接与调用状态）**不是线程安全的**，而网格预热用 4 线程 `QThreadPool` 并发的调 `facade.preview` → `decode_image_bytes` → `downloader.decrypt_image`，并发进原生层导致内存损坏，在**首次绘制（`show()`）**时崩——因为 `show()` 才触发第一次 paint → 入队 120 格 → 4 线程同时解密。修复：`RealMediaBackend` 增 `_decrypt_lock`（`threading.Lock`），`decode_image` 的两处 `decode_image_bytes` 调用串行化；昂贵的 Pillow 缩放/重编码仍在锁外，预热实际并发不受损。② **定位方法**（探针全部为临时脚本，验证后已删除）：`off`（关预热）/`sync`（GUI 线程内联解码）/`async`（真线程）三分对照 → 只有 `async` 崩；再按 `nofacade`（worker 不调 facade）/`oneworker`（池限 1 线程）/`nosignal`（真解码不 emit）细分 → `oneworker` 存活而 `nosignal` 仍崩，把凶手锁定为「`facade.preview` 的**并发**调用」而非 emit/信号/`show()` 本身。附带发现 `_ThumbRunnable` 被**重复定义**（前一份残留无 `run()`），已删除；`QThreadPool(self)` 改为无父对象 + `closeEvent` 里 `clear()+waitForDone()` 显式收摊；`_ThumbRunnable` 关 `setAutoDelete` 并由 `_warm_live` 字典持有至 `_warm_finish` 释放。③ **附带修复**：`_run_blocking` 的 `_FacadeWorker` 由「每次调用新建 `QThread`」改为**窗口级常驻单线程**（`_facade_worker()` 懒启动 + `_shutdown_worker()`），因为上游 session 建在该线程且持有线程亲和的原生状态，线程死了再使用会崩；`aboutToQuit`/`closeEvent` 双路 join。④ **e2e 脚本重写**：`scripts/verify_gallery_e2e.py` 由「`show()` + 手工 `processEvents()`」改为 **`QTimer.singleShot` 状态机 + 真 `app.exec()`**——无事件循环下 `show()` 顶层窗口在本平台不可用（进程静默终止，后续断言永不执行），这也是最初误判的成因。| 证据：`python scripts/verify_gallery_e2e.py`（`QT_QPA_PLATFORM=windows`，真实 10.4 万条账号）全 5 步 PASS——`grid tiles: 104029`、`model records: 104029 (no display cap)`、可见区 `0..120` 仅 47 格在解码、`decoded thumbnails in view: 72/121`、滚动到 52014 后可见区变 `51876..52116`、`worker joined cleanly` 且进程 EXIT=0 无 access violation；`pytest tests -q` 为 **346 passed, 1 skipped**（symlink 环境性）；`ruff check src tests` All checks passed。新增回归测试并**验证其会咬人**：`test_concurrent_decodes_never_enter_the_downloader_together`（去掉锁后确实 FAIL，且表现为缓存路径跨线程损坏而非法逃逸）、`test_blocking_steps_reuse_one_worker_thread`、`test_close_joins_the_worker_thread`、`test_shutdown_worker_is_idempotent`、`test_worker_started_only_when_a_blocking_step_runs`。| 仅只读访问真实微信目录；未写入源目录、未导出/删除任何真实文件，仅填充应用自身预览缓存。未据验证结果推断除 `windows` 与 `offscreen` 以外的平台行为。 |
| F-20260911-019 | 2026-09-11 | 三项真实反馈修复：网格照片缩放、筛选后解码顺序、数据目录自动检测。① **缩略图未被约束在固定框内**（用户报「照片缩放比例很有问题，完全没有被约束在固定框里」）：实测根因在 `QListView` 默认委托——`setGridSize`+`setIconSize` 只管布局格子，实际绘制由委托按图自身宽高比决定：方形图撑满整个单元格并**盖住自己的标题**，24px 小图仍按 24px 画（不放大），标题与图片重叠。修复：`gui/gallery.py` 新增 `MediaCellDelegate(QStyledItemDelegate)`，绘制权收归自己——固定 132×132 方框（`_THUMB_BOX`）+ `thumbnail_target_rect()` 等比 letterbox（小图放大、宽图上下居中、竖图左右居中、`setClipRect(box)` 兜底）+ 独立 34px 标题条（两行 `elidedText`，永不与图片重叠）+ 角落类型徽标（▶/▤/♪）+ 占位文案（载入中…/无法预览）；新增 `THUMB_STATE_ROLE` 让单元格区分 pending/ready/failed。② **筛选后解码顺序错乱且慢**（用户报「筛选图片后加载很慢，疑似不是按当前能显示的顺序渲染」）：探针实测确证——筛选时模型 reset 但**视图布局尚未重排**，`_render_grid` 立即取 `visible_range()` 得到 `(0,120)`，而布局稳定后用户实际看到的是 `(160,299)`（旧滚动偏移被 clamp 到新列表末尾）：**解码的是列表顶部，用户看的却是底部**。修复：`_render_grid` 改为 `scrollToTop()`（列表时间倒序，筛选后回到最新）+ 先 `_reset_warm_queue()` 丢弃属于旧列表的在排任务 + 再 `invalidate_visible_range()` 重算可见区；`MediaGalleryView` 连 `modelReset` → 置空缓存并 `QTimer.singleShot(0, ...)` 在布局稳定后补报一次（带 RuntimeError 兜底）；`_order_warm_queue_by_visibility` 由「二分 near/far」改为 `_row_distance` **按可见区距离全排序**（不在当前筛选内的记录排最后 `_WARM_FAR_DISTANCE`），队列再长也保证屏上格子优先。③ **数据目录自动检测**（用户报「能不能自动检测，不想每次都填地址」）：原 `_DEFAULT_ROOT_CANDIDATES` 只有 Documents 与 `C:/xwechat_files`，而用户真实根在 `D:\xwechat_files`（示例路径），故每次都探测不到、只能手填，且探测结果从不持久化。修复：新增 `gui/data_root.py`（`best_root` 按账号库字节选优 / `fixed_drive_letters` 经 Win32 `GetDriveTypeW` 只取本地固定盘，避免网络盘与空光驱阻塞 / `shallow_drive_candidates` 只扫盘根与盘根下一层、O(几十) 次 stat / `find_data_root` 优先级=显式 override > 上次成功 > 常规+浅扫合并排序）与 `gui/settings.py`（`%LOCALAPPDATA%/WeChatSpaceManager/settings.json`，原子写 temp+`os.replace`，损坏/不可写均降级不致命，路径可用 `WCSM_SETTINGS_PATH` 覆盖）；`RealReadOnlyWindow` 新增 `session_connected(str,str)` 信号（连接成功时发出）与 `prefer_account()`，`detect_accounts()` 在字段为空时先自动探测再报错；`launcher.launch()` 接线持久化，下次启动直接预填并选回上次账号。 | 真实数据（10.4 万条账号，`QT_QPA_PLATFORM=windows`）端到端 `scripts/verify_gallery_e2e.py` 全 PASS / EXIT=0：新增几何断言「600 格尺寸集合 = {[(148,174)]}」（修复前每格尺寸随图片宽高比漂移）、新增筛选断言「35198 张图 → scrollbar=0、可见区 0..120、队列首 12 行 = [6,7,8,…] 全在可见区内」；渲染实证：`grid_probe` 修复前红方块撑满整格并压住标题、24px 图不放大，修复后 6 种极端宽高比全部落在同一方框内；自动检测实证：本机 31ms 内返回 `D:\xwechat_files`（示例路径）（54 个浅扫候选、仅 1 个固定盘）；隔离 venv 全量 `pytest tests -q` 为 **373 passed, 1 skipped**（symlink 环境性；新增 27 项：gallery 几何/状态/渲染不越界 9、窗口筛选回顶/队列近优先/旧记录排后 3、`test_data_root.py` 设置与探测 12，另含既有 3 项更新）；`ruff check src tests` All checks passed；冻结包 `dist/wechat-space-manager-20260911T144222Z`（214.7 MB / 731 模块）构建+verify 通过（无 cleanup 路径、无写入面、无隐私模式），冻结 exe 自检 15 按钮全在白名单。 | 只读：仅访问真实微信目录读取，未写入源目录、未导出或删除任何真实文件，只填充应用自身缓存与新的 settings.json（后者存的是用户本来就要填的路径字符串）。浅扫仅覆盖盘根与盘根下一层（更深的自定义路径仍需手填一次，此后被记住）；设置文件记忆「上次成功的根」，卸载/移动微信目录后自动失效并回落到探测；筛选后固定回到列表顶部（时间倒序=最新在前），不回锚到原位置（有意，避免歧义）；委托接管绘制后不再使用 Qt 默认的图标/文字绘制路径，自绘项均走 `option.palette` 以跟随明暗主题。 |
| F-20260911-020 | 2026-09-11 | 四项用户反馈 + 标题年份：①**选中照片后每行从 4 格变 3 格**（仅小窗出现）。探针实测：预览列与网格同处一行，布局按 `sizeHint` 分配——`selection_label`/`preview_status` 是**换行 QLabel**，其 `sizeHint` 为整段文字宽度，且 `QLabel` 每次 `setText` 都会重置自身 sizePolicy（`setHeightForWidth(wordWrap)`），令 `QBoxLayout` 进入 HFW 模式按文字重算首选宽度；实测 `selection_label` 的 layout item hint 从 300 → 480（+100px），预览分组框 hint 404 → 504，网格宽度 580 → 480，即"选中一张照片少了一格"。修复：新增 `_StableHintLabel`（首选宽度恒定 + **在 `setText` 后重新钉住 `setHeightForWidth(False)`**）与 `_ImagePane`（`sizeHint`/`minimumSizeHint` 返回常量、图片缩放到当前尺寸并随 resize 重缩放）；实测分组框 hint 恒定 404、网格宽度恒定 580。**关键教训：`QSizePolicy.Ignored` 只让布局忽略该控件自己的 hint，挡不住兄弟控件；覆写 `sizeHint` 不配合 `setText` 后重钉策略会失效。** ②**Ctrl+滚轮调节缩略图大小**（明确不要列表/详情模式）：`MediaGalleryView` 增 `thumb_box`（离散档位 88…336，`_ZOOM_STEPS`）、`set_thumb_box`/`zoom_by`/`wheelEvent`（Ctrl+滚轮，普通滚轮仍滚动）、`thumb_box_changed` 信号；缩放时 `doItemsLayout()` 后把首个可见行 `scrollTo(PositionAtTop)` 锚定回顶部；解码分辨率按档位分桶（≤132→256 / ≤232→384 / 其余→512，`decode_edge_for`，缓存按 edge 键）；**放大越过已解码分辨率时清空缩略图重新解码（避免放大后糊），缩小则保留现有图**（`_on_thumb_box_changed`）；尺寸经 `thumbnail_size_changed` 信号由 launcher 持久化到 settings.json，下次启动恢复。③**时间筛选增加"XX天前"**：`old7/old30/old90/old180(半年)/old365(一年)`，语义=保留更早于该跨度的媒体（删旧照片的工作流方向），与"最近 N 天"并存；`_time_cutoffs` 改为返回 (keep_newer_than, keep_older_than) 两侧均可空。④**网格排序**：新增排序下拉（时间新→旧/旧→新、大小大→小/小→大、名称A→Z/Z→A），`_sort_records` 按 key 排序，切换即时 `apply_filters` 重排并回到顶部；名称排序大小写不敏感。⑤**标题补全年份**：单元格日期 `MM-DD` → `YYYY-MM-DD`，且日期放在大小之前（小格子右省略时保住年份、截掉大小）。附带：修掉关窗时偶发 `RuntimeError: Signal source has been deleted`（在飞预热 runnable 在窗口销毁后 emit，加 `_warm_stopped` 前置判断与 RuntimeError 捕获）。 | 真实数据（10.4 万条账号，`QT_QPA_PLATFORM=windows`）端到端 `scripts/verify_gallery_e2e.py` 全 PASS / EXIT=0，新增断言：标题含完整 `YYYY-MM-DD`、`set_thumb_size(232)` 后 300 格尺寸集合仍只有一个值 `[(248,274)]` 并恢复原尺寸、按大小排序后前 40 项严格递减（99,606,414 → 43,609,085）；布局稳定性实测（`pane_probe` 脚本，验证后已删）：修复前分组框 hint 404→504、网格 580→480，修复后两者恒定；隔离 venv 全量 `pytest tests -q` 为 **386 passed**（新增 12 项：gallery 缩放档位/锚点/滚轮 6、窗口布局稳定 2 + 时间档 1 + 排序 2 + 缩放联动 1）；`ruff check src tests` All checks passed；冻结包 `dist/wechat-space-manager-20260911T153336Z`（208.2 MB / 731 模块）构建+verify 通过，冻结 exe 自检 15 按钮全在白名单。已知环境性失败 2 项（`test_fixture_catalog.py`/`test_scanner.py` 的 reparse-point 用例，`is_symlink()` 在部分 shell 下为 False，属 symlink 能力漂移，与本轮改动的 gui 模块无任何 import 关系，Git Bash 下表现为 skipped）。 | 只读访问真实微信目录；缩放清空缩略图会触发一次可见区的重新解码（多耗缓存，属预期）；"XX天前"与"最近N天"共用 `now=UTC`（与既有筛选一致）；排序切换会整体重排（回到顶部）；`_StableHintLabel` 固定首选宽 300px，超长状态文字仍会换行显示但不再撑宽列。 |

| F-20260911-021 | 2026-09-11 | 用户六项反馈：①**选中照片后每行 4 格变 3 格**（仅小窗）。探针实测：预览列与网格同处一行，布局按 `sizeHint` 分配；`selection_label`/`preview_status` 是**换行 QLabel**，其 sizeHint 为整段文字宽度，且 **QLabel 每次 `setText` 都重置自身 sizePolicy（`setHeightForWidth(wordWrap)`）**，令 `QBoxLayout` 进入 HFW 模式按文字重算首选宽度——实测 `selection_label` 的 layout item hint 300→480（+100px）、预览分组框 hint 404→504、网格 580→480。修复：新增 `_StableHintLabel`（首选宽恒定 + **在 `setText` 后重新钉住 `setHeightForWidth(False)`**）与 `_ImagePane`（`sizeHint`/`minimumSizeHint` 返回常量、图片缩放随 resize）；修复后 hint 恒定 404、网格恒定 580。**教训：`QSizePolicy.Ignored` 只让布局忽略该控件自身的 hint，挡不住兄弟控件；覆写 `sizeHint` 不在每次 `setText` 后重钉策略会失效；排查必须打 layout item 层。** ②**Ctrl+滚轮调节缩略图大小**（明确不要列表/详情模式）：离散档位 88…336（`_ZOOM_STEPS`）、`set_thumb_box`/`zoom_by`/Ctrl+`wheelEvent`、锚点行 `scrollTo(PositionAtTop)` 保持；解码分辨率分桶 ≤132→256 / ≤232→384 / 其余→512（`decode_edge_for`，缓存按 edge 键）；**放大越档清空缩略图重新解码（防糊），缩小保留现有图**；尺寸经 `thumbnail_size_changed` 持久化到 settings.json。③**时间筛选增加"N天前"**：`old7/old30/old90/old180/old365`，语义=保留更早于该跨度；`_time_cutoffs` 返回 (keep_newer_than, keep_older_than)。④**网格排序**：排序下拉（时间/大小/名称各双向），切换即时重排回顶；名称大小写不敏感。⑤**标题补全年份**：`MM-DD`→`YYYY-MM-DD` 且日期置于大小前（小格右省略保年份）。⑥附带修复关窗偶发 `RuntimeError: Signal source has been deleted`。 | 真实数据（10.4 万条账号，`QT_QPA_PLATFORM=windows`）端到端 `scripts/verify_gallery_e2e.py` 全 PASS / EXIT=0，新增断言：标题含完整 `YYYY-MM-DD`、`set_thumb_size(232)` 后 300 格尺寸集合唯一 `[(248,274)]`、按大小排序前 40 项严格递减（99,606,414→43,609,085）；布局实测（`pane_probe`，验证后已删）：修复前分组框 hint 404→504、网格 580→480，修复后均恒定；全量 `pytest tests -q` 389 passed；`ruff` clean。 | 只读；缩放越档会触发一次可见区重解码（多耗缓存属预期）；"N天前"与"最近N天"共用 UTC now；排序切换整体重排回顶。 |
| F-20260911-022 | 2026-09-11 | 用户四项反馈：①**「预览缩略图」改名「预览图片」**（面板现在可显示缩略图或原图）+ **新增第三档「智能（小图用原图）」并设为默认**。前提先行验证：真机探针 `scripts/probe_auto_variant_speed.py`（真实账号 27,433 条 ≤1000KB 的图片中抽样 24 张）实测 **原图变体中位 0.180s / p90 0.515s / 最差 0.563s，24/24 成功**（缩略图中位 0.125s）——1000KB 阈值成立。实现：`real_session.resolve_preview_variant(record, variant)`（`auto` 按记录类型+大小零成本判定：仅 IMAGE 族且 ≤1MB → ORIGINAL，否则 THUMBNAIL；未知变体串降级 THUMBNAIL 不抛错），真实与假 facade 共用；原图解码失败时后端既有 `_thumbnail_fallback` 兜底。②**只有缩略图/源文件缺失/解不开的记录禁用「导出原图」与「导出后移入回收站」**，按钮 tooltip 说明原因（"仅有缩略图，无原图可导出"）；`_image_export_gate` 二道防线同步拦截；批量导出把这类记录计入 `skipped` 并逐条写原因，**导出后回收的批量绝不回收无原图记录**（没有备份的源文件保持不动）；worker 侧对 UI 线程未解析的可用性补判（每文件一次 stat，无解密，线程安全）。批量汇总本就报"跳过 N 项"，现在数字真实反映"多少张只有缩略图"。③**类型筛选新增「原图（可导出）」**：=图片族（IMAGE）且可用性=原图可用，与导出门控同一判定；即使"可用性"停在"全部"也自动按原图可用收窄。④**预览成功的大小显示错误修复**：原显示 `outcome.output_bytes`（解码缓存产物，几 KB 的 PNG），与"原图"字样矛盾；改为显示**源文件大小**（与格子一致、用户可验证的数字）。打包校验白名单同步「预览缩略图」→「预览图片」；用户手册重写至与当前界面一致（相册网格/缩放/排序/时间双向/智能预览/导出门控/数据目录自动检测）。 | 真机速度证据：24 张小图原图解码 median 0.180s / p90 0.515s / max 0.563s，全部成功（缩略图 median 0.125s）；全量 `pytest tests -q` **392 passed**（新增/更新 9 项：resolve 判定 2、窗口默认档与按钮名 1、小图走原图 1、缩略图禁用导出+tooltip 1、单文件拒绝 1、类型筛选原图 1、两个既有批量测试按新语义更新）；`ruff check src tests` All checks passed；打包校验白名单更新后 PASSED。 | 智能档只对 IMAGE 族生效（THUMBNAIL 记录本身就是小图，无"原图"可升）；≤1MB 是写死常量 `AUTO_ORIGINAL_MAX_BYTES`（用户给的建议值，已实测足够快）；批量导出对未知可用性的记录会做一次 stat（worker 线程，不冻结 UI）；「原图（可导出）」筛选会触发可用性 stat 遍历（与"可用性"筛选同成本，有进度条）。 |

| F-20260912-001 | 2026-09-12 | 用户需求：预览与管理里，单张选中时显示照片来自哪个聊天框（发送/接收最好也区分）。①**会话归属已实现**：`_conversation_of(record)`——`session_dir_of` 取 attach 目录名（=md5(username)）→ 已加载的 `_session_mapping` 反查 → `来自「X」（单聊/群聊）`；Rec 路径显示"…的合并转发记录"；文件/视频/语音按月存放 → 如实"该类型按月份存放，无法归属到具体会话"；映射缺失 → "未知会话（目录 8a8b…）"不显示空名。展示于 `_selection_summary`（selection_label 第二行，与按联系人筛选是同一个可逆映射的两面）。②**发送/接收不做**（用户确认"难做到就算了"）；本轮留下实测证据与脚本：`real_sender_id` 存在且可经上游 `_sender_id_index()` 解析（单聊中 sender=4/198 两值，4 疑似本人，跨会话复现）——**方向信号可用，卡点在"文件↔消息"关联**：图片消息 `message_content` 在 4.x 是压缩/加密二进制（非明文 XML、非 ZSTD 魔数），0/12 命中文件名；文件名 md5 对 server_id/local_id 的 7 种候选哈希 0/24 命中；消息时间与文件 mtime 相差极远不可作时间窗关联。后续若破解 message_content 格式（独立研究，类似 V2 容器），即可同时得到方向。探针脚本保留：`scripts/probe_message_direction.py`、`scripts/probe_name_join.py`。 | GUI 测试 4 项新增（单聊/群聊+合并转发/按月无法归属/未知会话）共 67 项窗口测试通过；全量 `pytest tests -q` 395 passed（2 项 reparse-point 环境性失败不变）；`ruff check src tests` All checks passed；e2e 新增 `check_conversation` 步骤但本轮未能重跑完成——凌晨连接阶段被阻塞（CPU 5 分钟仅 0.5s，微信进程在但密钥提取挂起，环境状态问题非代码回归），前一轮 e2e 已验证管线。 | 只读；会话归属只是把既有 mapping 换了个展示方向，无新数据读取；方向判定未实现（明确放弃，后续需独立破解 message_content 格式）。 |

| F-20260914-001 | 2026-09-14 | 用户报告：窗口竖向无法调整、最大化后底部按钮（导出等）找不到。**根因（探针实测）**：主窗口 `minimumSizeHint` 曾为 **1023x1129**，而屏幕可用区仅 1707x1019 —— 竖向被最小高度死锁（请求 720px 高被强制为 1129），最大化只有 996px 高、比最小高度还矮 133px，于是最底部控件被裁掉。3 个贡献项：右列 ③ 预览与管理最小高 **716px**（`preview_label.setMinimumSize(380,320)` + 8 个纵排按钮 + 11 处默认间距约 90px）、①/② 各 117px、④ 85px。**修复**：①右列内容包进 `QScrollArea(widgetResizable=True, 双轴 AsNeeded)`，按钮永远可滚达；②预览图最小尺寸 380x320 → 240x170；③右列 spacing 降至 4px；④中间行加 stretch=1（多余竖向空间给相册/预览，不再摊给各 group）；⑤显式 `setMinimumSize(860,560)`；⑥窗口标题带上版本号（`APP_VERSION` 此前是无人引用的死常量），便于用户报 bug 时对齐版本。**发布门禁加固**：`packaging/frozen_entry.py` 自检新增 `minimum_size`/`resized_to`/`clipped_buttons` 三项并计入 `ok`，`packaging/verify_artifact.py` 显式断言（最小高度 ≤700、1024x640 必须生效、无不可达控件，双轴判定）。 | 可达性探针（真实 `windows` 平台，测 1707x1019 / 1366x768 / 1280x720 / 1024x640 / 860x560 / 最大化 1707x996 六个尺寸）：窗口最小高 **1129 → 498**，各尺寸请求均被如实采纳，无不可达控件，四个 group 全部 inside=True；冻结包自检：`window minimum [1074, 480]`、`resized to 1024x640 -> [1024, 640]`、`unreachable controls: []`、标题含 v0.1.4；全量 `pytest tests -q` **475 passed / 1 skipped**；`ruff check .`（含 scripts/）All checks passed；产物 `dist/wechat-space-manager-20260914T093011Z` 校验 PASSED，发布 zip `wechat-space-manager-v0.1.4-win64-portable.zip`（88.5 MB）。新增 6 项回归测试：窗口可缩到 640 高、各尺寸按钮可达（含滚动容器兜底）、预览面板最小尺寸上限、预览列确为可滚动容器、标题含版本。 | 较小窗口（高低于约 700px）下右列仍需滚动才能看到最底部按钮——这是有意取舍（把纵向最小高度让给可用性），不是按钮消失；窗口宽度小到 860 时右列可能出现横向滚动条（冻结包实测最小宽 1074，Qt 样式/字体度量与源码环境略有差异）；所有尺寸数值来自本机 1707x1019 可用区的 Windows 平台与默认样式，其他 DPI/样式下绝对值可能不同（契约是可达成，而非具体像素）。 |
