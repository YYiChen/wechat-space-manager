# 协作守则

本仓库是一个仅本机运行的微信空间管理器。多 Agent 只参与开发；发布的软件不得把聊天内容、密钥、文件清单或遥测上传到任何服务。

## 工作内核

每项任务按以下闭环进行：

1. 从 `coordination/targets.yaml` 领取一个未被占用的目标，并在 `coordination/integration-board.md` 标记 `in_progress`。
2. 在 `plan/` 写出范围、依赖、验收证据和回滚方法；不得把实施细节只留在聊天中。
3. 仅编辑自己拥有的路径。需要改共享契约时，先取得 `coordination/contract-lock.yaml` 的锁并更新版本。
4. 用夹具或副本验证，再将可复核的命令、结果和未解决项追加到 `coordination/fact-log.md`。
5. 在集成板登记交付物、验证证据和风险，释放目标及契约锁。将可复用经验写入 `docs/experience-extraction.md`。

事实日志只追加，不能改写或删除既有条目。计划、实现、验证和事实必须彼此可追溯。

## 文件所有权

所有权以 `coordination/agents.yaml` 为准。一个路径同一时间只能有一个写入者。没有明确所有权的共享文件包括：

- `AGENTS.md`
- `README.md`
- `.gitignore`
- `coordination/*`
- `plan/*`
- `docs/*`

机器可读的精确路径映射见 `coordination/path-ownership.yaml`；并行 Agent 的分支和
worktree 规则见 `coordination/branch-policy.md`。默认策略是未登记路径不得写入。

共享文件只能由协调 Agent 修改；其他 Agent 提交建议或在集成板标记 `needs_decision`。不要顺手格式化、重命名或覆盖其他 Agent 的工作。

## 共享契约锁

`contracts/` 中的公开模型、枚举、序列化格式和错误码是共享契约。修改它们前必须：

1. 在 `coordination/contract-lock.yaml` 取得契约名的独占锁；
2. 说明兼容策略、受影响消费者和迁移/回滚方式；
3. 更新 schema 版本与夹具；
4. 由集成 Agent 执行跨模块验证后才可释放锁。

没有锁，不得编辑 `contracts/`，也不得通过假定字段绕过契约。

## 风险等级与操作边界

| 等级 | 示例 | 要求 |
| --- | --- | --- |
| R0 | 文档、合成夹具、纯函数 | 常规单元测试 |
| R1 | 只读扫描、索引、缩略图缓存 | 路径边界测试；不得写微信目录 |
| R2 | 数据库副本、密钥在内存中使用、导出媒体 | 只对副本操作；密钥不得落盘或写日志 |
| R3 | 隔离移动、回收站、缓存批量清理 | 不变量检查、逐文件收据、可恢复路径、人工确认 |
| R4 | 永久删除、修改微信目录、任何数据库写入 | 默认禁止；仅由非 Agent 执行器在用户逐次确认后执行，且需独立复核 |

### 高风险删除硬规则

- 代码、测试和开发 Agent **不得**删除真实微信数据。
- 运行微信进程存在、扫描清单过期、路径逃出批准账号根目录、映射置信度不足或用户未确认时，执行器必须拒绝操作。
- `db_storage`、聊天数据库、收藏、`business/favorite`、未知归属文件、正在写入的文件一律不在第一版删除范围。
- 永久删除不得由 UI、规划 Agent 或大模型直接调用；只能消费不可变 `CleanupPlan`，并写出逐文件 `CleanupReceipt`。
- 删除前后都要验证规范化绝对路径、文件标识、大小、修改时间和计划摘要；任一不匹配即跳过并记录。

## 私密数据

真实微信目录、账号 ID、联系人、解密数据库、密钥、内存转储、图片/视频/文件、程序缓存、审计收据和本机绝对路径均视为私密数据。它们不得进入 Git、Issue、截图、测试快照、日志或示例。测试只使用合成数据；私有验收夹具仅可在本机手工运行并只记录聚合结果。

## 交接格式

交接消息必须包含：目标 ID、改动文件、验证命令及结果、风险等级、契约版本/锁状态、未解决项。未提供验证证据的任务不得标为完成。
